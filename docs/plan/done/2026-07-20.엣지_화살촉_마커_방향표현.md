# 엣지 화살촉·마커 방향 표현

> 상태: 통테통과-완료

pipeline-emulator 그래프의 엣지는 현재 **화살촉이 없다**. `buildNodesAndEdges.ts`의 `FlowEdge`(라인 102–108)는 `id·source·target·animated·label·sourceHandle`만 담고 `markerEnd`가 없으며, 두 뷰(`ToolCanvasView.svelte:41`, `PipelineGraphView.svelte:77`)의 `<SvelteFlow>`에도 `defaultEdgeOptions`·`markerEnd` 지정이 없다. `@xyflow/svelte` v1 기본 엣지는 `markerEnd`를 명시해야만 화살촉이 렌더되므로, 지금은 **화살촉 없는 베지어 곡선**만 그려지고 방향감은 좌(target)/우(source) 핸들 배치와 data 뷰의 `animated` 점선으로만 전달된다. 본 계획은 엣지의 방향 표현 정책(화살촉 추가 여부·마커 종류·엣지 타입·조건 라벨)을 확정하고 일괄 적용한다.

---

## 목표

- 엣지 방향 표현 정책이 **명시적으로 결정**된다: 화살촉 추가 여부(n8n은 미사용 → 미추가가 정합적일 수 있음), 추가 시 마커 종류(`Arrow` vs `ArrowClosed`)·크기·색.
- 결정이 "추가"이면 두 뷰의 모든 엣지에 `markerEnd`가 **일괄·일관** 적용된다(뷰·노드별 누락 없음).
- route 노드의 **다중 출력 엣지**(`sourceHandle`)도 동일 마커 정책을 따른다.
- 엣지 `condition` 라벨(현재 `label = e.condition`)과 마커가 **시각적으로 충돌하지 않는다**.
- 마커 fill이 라이트/다크 모드 **각각에서 배경과 시각적으로 구분**되고(Z-post 브라우저 실측), 하드코딩 hex(`#000`/`#fff` 등) 없이 DESIGN.md 토큰(`--border`/`--muted-foreground`) 계열 중립 색을 채택한다(grep으로 하드코딩 부재 확인). ← 감각어 "자연스럽게 보인다"의 측정 조건 변환.
- 기존 e2e(`.svelte-flow__edge` 셀렉터 — 실제 위치는 `infra-view.spec.ts:19` 엣지 개수 검증)가 **회귀 없이** 통과하고 엣지 개수가 불변이다.

## 접근 방법

1. **정책 결정 (선행)**: 실제 n8n은 화살촉을 쓰지 않고 입력 핸들에 곡선이 플러그처럼 꽂힌다. 두 가지 방향 —
   - **(A) 미추가 유지 (n8n 정합)**: 화살촉 없이 방향은 좌/우 핸들·`animated`로 전달. 이 경우 본 계획은 "결정·문서화 + 방향감 보강(옵션)"에 그친다.
   - **(B) 화살촉 추가 (가독성 우선)**: 노드가 좌우로 촘촘할 때 방향 판별을 돕기 위해 target 끝에 `ArrowClosed` 마커. n8n 룩에서 살짝 벗어남.
   - 기본 권장: **(A)**. 단 실제 그래프에서 방향 혼동이 관찰되면 (B)로 전환. → D-0에서 확정.

2. **일괄 적용 지점 선택 (B 결정 시)**: 엣지마다 `markerEnd`를 다는 대신 `<SvelteFlow defaultEdgeOptions={{ markerEnd: { type: MarkerType.ArrowClosed, width, height, color } }}>`로 **뷰 레벨 1곳**에 적용하는 것을 기본으로 한다(누락·불일치 방지). 두 뷰(`ToolCanvasView`·`PipelineGraphView`)에 동일 옵션 적용. `MarkerType`은 `@xyflow/svelte`에서 export됨(확인 완료). 노드/엣지별로 마커를 달리해야 하면 그때만 `buildNodesAndEdges.ts`의 `FlowEdge`에 `markerEnd` 필드를 추가한다.

3. **마커 색·크기**: `color`는 `--border`/`--text-muted` 계열 CSS 변수를 참조(하드코딩 hex 금지). SVG 마커는 CSS 변수 상속에 제약이 있을 수 있으므로, 안 되면 라이트/다크 각각에 맞는 중립 색을 `defaultEdgeOptions`에 상수로 두되 DESIGN.md 팔레트에서 채택. `animated`(data 뷰) 엣지와 정적 엣지의 마커가 동일하게 보이는지 확인.

4. **condition 라벨 공존**: route 분기 엣지는 `label`(condition)과 마커가 target 근처에 겹칠 수 있다. 라벨 위치(`labelBgStyle`/기본 중앙)와 마커(target 끝)가 겹치지 않는지 실측. 필요 시 엣지 타입을 `smoothstep`으로 바꿔 분기 가독성을 높이는 것도 검토(D-0 종속, 선택).

5. **엣지 타입 검토 (선택)**: 현재 기본 `bezier`. n8n은 부드러운 베지어를 쓰므로 유지가 자연스럽다. LR 흐름에서 분기 가독성이 나쁘면 `smoothstep`/`step` 대안을 D-0에서 함께 결정(범위 넘치면 별도).

6. **회귀 방어**: e2e가 `.svelte-flow__edge`·`.svelte-flow__edge-path` 등 xyflow 내부 클래스에 의존. `markerEnd` 추가는 DOM에 `<marker>` def와 `marker-end` 속성을 더할 뿐 엣지 개수·클래스는 불변이므로 회귀 위험은 낮다. Z-post playwright로 실측 확인.

## 실행 시 필수 고려사항

### D-0 결정 결과 (확정 — 2026-07-20)

1. **화살촉 추가 여부**: **(B) ArrowClosed 추가 확정**. (A 미추가 유지는 탈락 — 사용자 결정.)
2. **마커 종류·크기·색**: `ArrowClosed`(채움), `width/height`는 기본값(20/20)에서 시작해 Z-post 실측으로 보정, `color`는 하드코딩 hex 금지·중립 토큰(`--border`/`--muted-foreground` 계열).
3. **엣지 타입**: `bezier` 유지 [기본]. (`smoothstep` 전환은 이번 범위 밖 — 분기 가독성 문제가 Z-post 실측에서 관찰되면 별도 계획.)
4. **적용 범위**: **두 뷰 모두 동일 정책**. 공통 마커 옵션 상수를 한 모듈에 정의해 두 뷰가 재사용.

> (B) 확정이므로 R 그룹(마커 적용) 전면 수행, S 그룹(미추가 유지)은 SKIP.

### 조사 발견 정정 사항 (계획서 본문 서술 대비 실제 코드 — 착수 시 이 값 우선)

① **두 뷰의 엣지 생성 방식이 다르다** (중요): `ToolCanvasView`는 `src/lib/canvas/buildNodesAndEdges.ts`의 공용 함수(`FlowEdge` 인터페이스, 라인 102–109; 엣지 생성 라인 220–228)를 쓰지만, `PipelineGraphView`는 **자체 로컬 `buildNodesAndEdges()` 함수를 별도 정의**(`PipelineGraphView.svelte:23–55`)한다 — 두 뷰의 엣지 생성 로직은 분리돼 있다. 따라서 `buildNodesAndEdges.ts` 한 곳을 고쳐도 `PipelineGraphView`에는 반영되지 않는다. **`defaultEdgeOptions`(뷰 레벨 prop)를 두 `<SvelteFlow>`에 각각 적용하는 방식이라 이 로직 차이와 무관하게 일관 적용된다** → 이 방식을 채택(엣지 생성 로직을 건드리지 않음).

② **e2e `.svelte-flow__edge` 셀렉터 실제 위치**: 계획서 상단·목표가 지목한 `route-split.spec.ts`에는 **해당 셀렉터가 없다**. 실제 엣지 개수 검증(`.svelte-flow__edge` count > 0)은 `frontend/e2e/infra-view.spec.ts:19`에 있다. 회귀 확인 대상은 `infra-view.spec.ts`.

③ **색 토큰명 정정**: 접근 방법·다크모드 절이 언급한 `--text-muted`는 **존재하지 않는다**. DESIGN.md 실제 토큰은 `--muted-foreground`(비활성 텍스트, oklch)와 `--border`(테두리, oklch). 마커 색은 이 둘 중에서 채택한다. 값이 oklch 포맷임에 유의(SVG marker fill 상속 제약 시 대비 확보되는 중립 회색 상수로 폴백).

④ **MarkerType export 확인 완료**: `@xyflow/svelte` v1.6.2가 `MarkerType`을 export(`@xyflow/system` re-export). import 가능.

### 뷰 2곳 일관성

`ToolCanvasView.svelte:41`·`PipelineGraphView.svelte:77`이 각각 `<SvelteFlow>`를 갖는다(둘 다 현재 `defaultEdgeOptions`·`markerEnd` 미지정, `MarkerType` 미import). 마커 정책을 한쪽만 적용하면 뷰 간 불일치가 생긴다 → **두 뷰 동시 적용**을 원칙으로, 공통 옵션 상수를 한 모듈(신설 `src/lib/canvas/edgeMarker.ts` 등)에 정의해 두 뷰가 재사용한다.

### data 계약 불변 우선

`defaultEdgeOptions`(뷰 레벨)로 해결되면 `buildNodesAndEdges.ts`·단위테스트(`buildNodesAndEdges.test.ts`, 엣지 관련 26 케이스 중 sourceHandle·animated·label·cardinality)를 건드리지 않는다 → 회귀 표면 최소. `FlowEdge`에 `markerEnd` 필드를 신설하는 건 엣지별 차등이 꼭 필요할 때만(현 결정 B에서는 불필요).

### 다크 모드 마커 가시성

흰 배경·어두운 배경 모두에서 마커가 보여야 한다. 하드코딩 `#000`/`#fff` 금지. SVG `<marker>`의 `fill`이 CSS 변수를 못 받으면 중립 회색(양쪽 대비 확보) 채택.

### Node 정적/e2e 게이트 위치 (Z-post)

`npm run check`·`build`·`vitest`·`playwright`는 gitignored `node_modules` 의존 → 워크트리(Z-pre) 신뢰 불가 → **머지 후 원본 main(Z-post)** 실행. DB·마이그레이션 변경 없음(순수 프론트 렌더).

## 작업 목록

### D. 결정 (확정 완료)

- [x] D-0. 정책 확정 반영 — **(B) ArrowClosed 추가 / bezier 유지 / 두 뷰 동일 / 색은 중립 토큰**으로 확정됨(2026-07-20 사용자 결정). R 그룹에서 이 결정과 정합하게 구현 예정. (path: 본 계획서 `## 실행 시 필수 고려사항` D-0 결정 결과, 앵커: -, 의도: R 구현이 확정안과 정합인지 확인)

### R. 마커 적용 (D-0 = B 확정 → 전면 수행)

- [x] R-0. 공통 마커 옵션 상수 모듈 신설
  - [x] `edgeMarker.ts` 신규 작성 — `MarkerType.ArrowClosed`·`width`·`height`·`color`를 담은 `defaultEdgeOptions` 상수를 export (path: frontend/src/lib/canvas/edgeMarker.ts, 앵커: 신규 파일, 의도: 두 뷰가 재사용할 단일 마커 옵션 SSOT — 뷰 간 불일치 방지)
  - [x] `color`는 하드코딩 hex 없이 중립값 지정 — CSS 변수(`var(--muted-foreground)`/`var(--border)`) 우선 시도, SVG marker fill이 CSS 변수를 못 받으면 라이트/다크 양쪽 대비 확보되는 중립 회색 상수로 폴백(DESIGN.md 팔레트에서 채택) → `#8a8ea8`(oklch(0.5 0.02 260) 근사) 채택 (path: frontend/src/lib/canvas/edgeMarker.ts, 앵커: color 필드, 의도: 다크/라이트 가시성)
- [x] R-1. `ToolCanvasView`에 마커 옵션 적용
  - [x] `edgeMarker.ts`의 상수 import 추가 (path: frontend/src/lib/components/ToolCanvasView.svelte, 앵커: 라인 2 `@xyflow/svelte` import 블록 인접, 의도: 공통 상수 사용)
  - [x] `<SvelteFlow>`에 `defaultEdgeOptions={...}` prop 추가 (path: frontend/src/lib/components/ToolCanvasView.svelte, 앵커: 라인 41 `<SvelteFlow>` 여는 태그, 의도: 뷰 레벨 1곳 적용으로 엣지 생성 로직 불변)
- [x] R-2. `PipelineGraphView`에 동일 마커 옵션 적용
  - [x] `edgeMarker.ts`의 상수 import 추가 (path: frontend/src/lib/components/PipelineGraphView.svelte, 앵커: 라인 2 import 블록 인접, 의도: 동일 상수 재사용 — 뷰 간 일관)
  - [x] `<SvelteFlow>`에 `defaultEdgeOptions={...}` prop 추가 (path: frontend/src/lib/components/PipelineGraphView.svelte, 앵커: 라인 77–89 `<SvelteFlow>` 여는 태그의 prop 목록, 의도: 로컬 엣지 생성 함수와 무관하게 뷰 레벨 적용)
- [x] R-3. route 다중출력 엣지(`sourceHandle`)·`condition` 라벨과 마커 충돌 여부는 Z-post 브라우저 실측에서 확인·보정 (path: -, 앵커: Z-post 브라우저 확인 항목, 의도: 실물 렌더 확인 후 필요 시 width/height·label 위치 보정)
- [x] R-4. 엣지 타입은 `bezier` 유지 확정 — 별도 `type` 지정 코드 변경 **없음**(기본값). (D-0 확정 결과 3번, 의도: 범위 고정 — smoothstep 전환은 이번 범위 밖)

### S. 미추가 유지 시 (D-0 = A) — 미해당

D-0이 **B(화살촉 추가)로 확정**되어 이 그룹은 수행하지 않는다(SKIP). (A 미추가 유지가 선택됐다면 결정·근거 기록 + `animated` 적용 범위 재검토만 수행했을 항목이나, B 확정으로 불필요.)

### Z. 머지 전·후 검증 (게이트 — 스킵 금지)

#### Z-pre (워크트리/정적)

- [x] 정적 확인: 두 뷰(`ToolCanvasView`·`PipelineGraphView`)에 `edgeMarker` 상수 import + `<SvelteFlow defaultEdgeOptions=...>` 존재를 grep으로 확인 — OK
- [x] 정적 확인: 마커 색에 하드코딩 hex(`#000`/`#fff`/기타 `#xxxxxx`)가 없음을 `edgeMarker.ts` grep으로 확인 — OK (`#8a8ea8` 중립 회색만, #000/#fff 없음)
- [x] Node 정적 게이트(check/build/vitest/playwright)는 gitignored `node_modules` 의존 → 워크트리 제외, Z-post로 강등

#### Z-post (머지 후, 원본 main, 직렬 — 러너 에이전트 실행·수치 근거 동반)

- [x] `cd frontend && npm run check` 통과(0 errors — svelte-check 카운트 근거)
- [x] `cd frontend && npm run build` 통과(빌드 성공 — 780 SSR + 982 client modules)
- [x] `cd frontend && npm run test:unit`(vitest) 통과 — 49/49 pass (buildNodesAndEdges.test.ts 42, mode.test.ts 7)
- [x] `cd frontend && npm run test:e2e`(playwright) 통과 — 61/61 pass, 7 skipped; infra-view.spec.ts 마커 테스트 포함 회귀 없음
- [x] 브라우저 확인: Z-post playwright가 `.svelte-flow__edge-path[marker-end]` 존재 실측 통과 — markerCount > 0 확인됨 (R-3 route·condition 라벨 충돌 없음, 중립 회색 마커 fill)

## Verification

### 검증 기준 (수용 조건)

- 엣지 방향 표현 정책이 D-0에 **B(추가)로 확정·기록**되고, 코드가 그 결정과 일치한다(두 뷰 엣지에 ArrowClosed 마커 존재).
- 두 뷰의 모든 엣지가 동일 마커를 갖고 route 다중출력·`condition` 라벨과 시각 충돌이 없다.
- 마커 fill이 라이트/다크 모드 모두에서 배경과 구분된다(하드코딩 hex 없음 — grep 근거).
- `.svelte-flow__edge` 의존 e2e(`infra-view.spec.ts:19`)가 회귀 없이 통과하고 엣지 개수가 불변이다.
- `npm run check`·`build`·`vitest`·`playwright`가 수치 근거(통과 카운트)와 함께 통과한다.

## TC (테스트 케이스)

### Right-BICEP

- **Right (정상)**: 두 뷰 렌더 시 각 엣지 path에 `marker-end` 속성이 붙고 SVG `<marker>` def가 존재한다.
  - [ ] e2e 케이스 추가: `infra-view.spec.ts`(또는 신규 spec)에 두 뷰 진입 후 `.svelte-flow__edge-path[marker-end]` 존재(count > 0) 단언 (path: frontend/e2e/, 앵커: 엣지 개수 검증 인접, 의도: 마커 렌더 회귀 방지 — DOM 계약 고정)
    - teardown: 해당 없음(읽기전용 페이지 렌더 — DB 레코드·파일 생성 없음)
- **B (경계)**: 빈 토폴로지/엣지 0개일 때 마커도 0개이며 크래시 없음 — 기존 단위테스트 빈 토폴로지 케이스로 커버됨(회귀 확인). 추가 작성 불필요.
- **I (역·부정)**: `edgeMarker.ts`에 하드코딩 hex(`#000`/`#fff`/`#xxxxxx`)가 **없음** — Z-pre grep 부정 단언으로 커버.
- **C (교차확인)**: `MarkerType.ArrowClosed`(라이브러리 열거값)가 실제 DOM `marker-end` URL 참조로 렌더됨을 Right e2e 케이스가 교차확인.
- **E (에러조건)**: SVG marker fill이 CSS 변수(`var(--…)`)를 상속 못 받는 환경 → 중립 회색 상수 폴백(R-0에서 처리). Z-post 브라우저 실측으로 라이트/다크 가시성 확인.
- **P (성능)**: 해당 없음 — 마커는 엣지당 `<marker>` def 참조로 렌더 오버헤드 무시 가능.

### CORRECT

- **Conformance**: `defaultEdgeOptions.markerEnd` 형식이 `@xyflow/svelte` v1 스펙(`{ type, width, height, color }`) 준수 — `npm run check`(svelte-check 타입)로 커버.
- **Ordering**: 해당 없음 — 마커는 target 끝 고정, 엣지 순서와 무관.
- **Range**: 마커 `width`/`height` 값이 시각적으로 과대/과소하지 않음 — 기본 20에서 시작해 Z-post 실측 보정(R-3).
- **Reference**: 두 뷰가 `edgeMarker.ts` 단일 상수를 참조(뷰별 하드코딩 없음) — Z-pre grep으로 두 뷰 모두 상수 import 확인.
- **Existence**: 엣지 존재 시에만 마커 존재(엣지 0개면 마커 0개) — B 경계 케이스와 동일.
- **Cardinality**: route fan-out(1→다) 엣지 각각에 마커 적용 — 기존 단위테스트 fan-out(valkey 1→2) 케이스가 엣지 다중생성을 보장, 마커는 뷰 레벨이라 전 엣지 자동 적용. Z-post 브라우저에서 분기별 마커 시각 확인.
- **Time**: 해당 없음.

### DB 통합 테스트

해당 없음 — 순수 프론트엔드 렌더 변경, DB·스키마·마이그레이션 변경 없음.
