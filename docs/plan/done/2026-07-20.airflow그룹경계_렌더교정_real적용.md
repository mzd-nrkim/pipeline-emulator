# Airflow 그룹 경계 렌더 교정 + real 모드 적용

> 상태: 통테통과-완료

데이터흐름 그래프에서 "Airflow가 한 스텝으로 돌고 끝난 뒤 presidio 등 스테이지가 이어지는" 것처럼 보이는 문제. 실행 실체는 presidio·docling·kure가 Airflow worker 태스크 안에서 도는 스테이지(`silver_2_masking.py:65` in-process `pii_engine` 호출, 별도 서비스 아님)인데, 그래프는 이를 반영하지 못한다. 재설계(`done/2026-07-20.실행주체구조_캔버스_재설계_airflow오케스트레이션.md`)가 sample(`mockTopology`)에만, 그것도 **렌더가 깨진 채로** 부분 착지했고, real(`realTopology`)은 옛 선형 peer 모델 그대로다.

현 상태(근거 확인 완료):
- **sample** (`frontend/src/lib/mock/topology.ts`): presidio·docling·kure에 `parentId:'node-airflow-group'` 있음(그룹 합성됨) → 그러나 **자식 노드가 서로 겹쳐 렌더**. 원인은 `buildNodesAndEdges.ts`가 자식에 **절대 위상좌표**(`:94` `x:d*COL_GAP`)를 주면서 `parentId`+`extent:'parent'`(`:155`)를 붙이는데, xyflow는 자식 `position`을 **부모 상대좌표로 재해석**한다 → 부모 원점 차감(상대변환) 누락 + 작은 그룹 박스(`:216-220`)의 `extent:'parent'` 클램프로 자식 3개가 박스 우측 경계에 포개진다.
- **sample** 추가 결함: 독립 `node-airflow` 노드(`:116-126`, 가상 `dagId:'lllm_pipeline'`)가 체인에 남아 `s3-bronze→node-airflow→docling`(`:280,283`)로 배선 → airflow가 "한 스텝"(허구)과 "경계 박스"(실체)로 **이중 표현**.
- **real** (`frontend/src/lib/mock/realTopology.ts`): `parentId`/`group` **0건**. 순수 평면 선형 체인(`:272-283` `s3-bronze→node-airflow→docling→presidio→kure→mock-api→{es→es-search, mysql}`). airflow 경계화 미적용.
- 부수: `node-mock-api.tool`이 두 파일 모두 `'presidio'` 오분류(sample `:252`, real `:244`) — 실제는 enrichment.

---

## 목표

- sample 데이터흐름 그래프에서 Airflow 그룹 박스 안 자식 노드(docling·presidio·kure)가 **겹치지 않고 가로로 나란히** 렌더된다.
- 독립 `node-airflow` "한 스텝" 노드가 제거(또는 그룹 경계로 흡수)되어, airflow가 그래프에서 **오직 경계 박스로만** 표현된다 — 이중 표현 제거.
- `realTopology`에도 동일한 그룹 경계 모델이 적용되어, real 모드 데이터흐름도 presidio·docling·kure를 Airflow 경계 안 스테이지로 표시한다.
- `node-mock-api.tool` 오분류(`presidio` → 실제 enrichment tool)를 두 토폴로지에서 정정한다.
- 위 변경 후에도 nodeclick 드릴다운·infra 뷰·엣지 viaTable 라벨이 정상 동작하고, 관련 테스트가 재설계 구조 기준으로 통과한다.

## 접근 방법

1. **좌표 상대변환 교정 (`buildNodesAndEdges.ts` — 핵심 버그)**
   - 자식 노드(`parentId` 보유)의 `position`을 **부모 그룹 원점 기준 상대좌표**로 변환한다. 그룹 박스(minX/minY)를 먼저 확정한 뒤 자식 `position.x -= groupX`, `position.y -= groupY` 재계산, 또는 그룹 내부 전용 레이아웃(자식만 별도 0-origin 가로 배치)으로 산출한다.
   - 그룹 박스 width/height를 **상대좌표 기준 자식 실측 폭 + 노드 크기(≈200×?) + 패딩**으로 재계산해 `extent:'parent'` 클램프가 자식을 접지 않도록 한다.
   - depth 계산(`computeDepths`)이 그룹 자식을 전체 그래프 depth에 넣는지 / 그룹 내부에서 재정렬하는지 결정(아래 D-1).

2. **독립 `node-airflow` 처리 (양 토폴로지)**
   - `node-airflow` 노드를 제거하고 진입 엣지를 `s3-bronze → docling`(그룹 첫 자식)으로 재배선. dependency 엣지(mysql/seaweedfs/valkey/es → node-airflow)는 **그룹 노드(`node-airflow-group`) 대상**으로 옮기거나 대표 스테이지로 재연결(아래 D-2).
   - 그룹 노드 메타(`buildNodesAndEdges.ts:206` `Airflow (CeleryExecutor)`)가 trigger·config를 표현하도록 필요한 필드를 그룹 노드로 이전(트리거 버튼이 airflow 노드에 걸려 있었다면 그룹으로).

3. **`realTopology` 그룹 모델 적용**
   - real 노드 presidio·docling·kure에 `group:'airflow'`+`parentId:'node-airflow-group'` 부여, 진입 엣지 재배선(2와 동일 패턴). real 고유 노드(`node-es-search` 등)·실제값(executor=SequentialExecutor, dagId 실제 목록, recognizers=phone,rrn,email,bank_account, nlpEngine=ko_core_news_lg)은 보존.
   - gold_4(mock-api)·gold_5(es)의 그룹 포함 여부는 D-3 결정.

4. **`node-mock-api.tool` 오분류 정정** — 두 토폴로지에서 `tool:'presidio'`를 실제 enrichment tool id로 교체(카탈로그에 적절 엔트리 확인, 없으면 displayNameOverride 유지 + tool id 정정). 아이콘/셀렉터 회귀 확인.

5. **테스트·기대값 재작성** — `buildNodesAndEdges.test.ts`(그룹 좌표·자식 상대위치·그룹 박스 크기), nodeclick/infra/graph-handle 관련 e2e를 변경 구조 기준으로 갱신. 단순 통과가 아니라 상대좌표·경계 배선 기대값 명시.

## 결정 항목 (착수 전 확정)

- **D-1 그룹 내부 레이아웃**: (a) 전체 그래프 depth 유지하되 상대변환만(그룹 폭이 전체 depth 간격에 종속) vs (b) 그룹 자식을 0-origin에서 가로 재배치(그룹 내부 독립 레이아웃). → 기본안 (b): 겹침 근본 차단, 그룹 폭 예측 가능.
- **D-2 dependency 엣지 재대상**: node-airflow 제거 시 `mysql/seaweedfs/valkey/es → node-airflow` dependency 엣지를 (a) `node-airflow-group`으로 vs (b) 유지 위해 airflow 노드를 그룹 노드로 대체. → 기본안 (a).
- **D-3 gold_4·gold_5 그룹 포함**: mock-api=외부 HTTP 서비스(경계 밖 + HTTP dependency 유지), es=gold_5 airflow DAG(경계 안 후보). → 기본안: mock-api 경계 밖 유지, es는 경계 밖 유지(색인 sink 성격)하되 재설계 원안과 정합 재확인.
- **D-4 xyflow group 노드 렌더**: `AirflowGroupNode.svelte` 이미 존재 — `ToolCanvasView.svelte:50`이 `nodeTypes={{ tool, group: AirflowGroupNode }}`로 등록하고 pipeline 라우트(`routes/[mode=mode]/pipeline/+page.svelte:215`)가 ToolCanvasView를 렌더하므로 그룹 노드는 렌더된다. onnodeclick은 `node.type === 'group'`이면 early-return(그룹 박스 자체 비선택), 자식(type `tool`)은 드릴다운 동작 — 상대변환 후에도 자식 핸들 좌표계·nodeclick이 정상인지 e2e 실측 필요. (실물 확인: `PipelineGraphView.svelte:81`은 `tool`만 등록 — group 미등록이나 pipeline 라우트 미사용이므로 무관.)
- **D-5 `node-mock-api.tool` 정정 방식 (실물 확인 후 결정)**: `toolCatalog.ts`에 enrichment/mock-api 엔트리 **없음**(등록 id 14종: apache-nifi·debezium·dam·apache-airflow·presidio·docling-langchain·kure-embedding·valkey·airflow-branch·s3·mysql·elasticsearch·kibana·zookeeper). → (a) 카탈로그에 `mock-api`/`enrichment` 엔트리 신규 추가(아이콘·vendor·accent 정의) vs (b) `displayNameOverride:'Mock API (Enrichment)'` 유지 + `tool`을 폴백 안전 id(미등록 → `❓`/Unknown 폴백)로. → 기본안 (a): 오분류 근본 제거 + 카드 아이콘 정합. 단 `presidio` 오분류로 인한 기존 아이콘/셀렉터 회귀 확인 필요.

## 실행 시 필수 고려사항

> plan-review 검토 중 실물 코드와 계획 전제가 상충하거나 환원 불가한 발견 사항. 구현 전 반드시 실물을 재확인한다.

- **① infra 뷰 회귀 (핵심 — 접근 방법 #2 재검토)**: 접근 방법 #2/D-2는 `node-airflow` 노드를 **완전 제거**하고 dependency 엣지를 합성 그룹 노드 `node-airflow-group`으로 재대상한다고 하나, `node-airflow-group`은 `buildNodesAndEdges.ts:200-247`이 **data 뷰에서만** 합성하는 노드다(`if (view === 'data')`). infra 뷰에는 존재하지 않으므로, dependency 엣지(`mysql-container/seaweedfs/valkey/es/mock-api → node-airflow`)를 그룹으로 재대상하면 **infra 뷰에서 채널 필터(`:57-62`)에 걸려 전부 사라진다**. 또한 기존 테스트 `buildNodesAndEdges.test.ts:136-164`가 infra 뷰에서 `node-airflow` 노드 존재를 단언 → 노드 삭제 시 회귀. → **권장 대안**: node-airflow 노드는 **보존**하되 **data-channel 엣지만 제거**한다(`s3-bronze→airflow`·`airflow→docling` 삭제 후 `s3-bronze→docling` 추가). data 뷰에서는 orphan 필터(`:70-72`)로 자동 숨김(그룹 박스가 대체 표현), infra 뷰에서는 dependency 노드로 그대로 표시. 이 경우 **D-2 dependency 재대상 자체가 불필요**하다. (실물 확인 후 결정: node-airflow 삭제 vs data-edge만 제거)
- **② `.group` 필드는 데드**: `buildNodesAndEdges.ts`는 그룹 소속을 **`parentId`로만** 판정한다(`:150,204,217`). topology.ts의 `group:'airflow'`(`:132,146,160`)는 현재 미사용(draft `2026-07-20.bug-buildNodesEdges-groupIds-데드코드.md`에서 `.group` 순회 데드코드 제거 완료, 머지완료-통테대기). 따라서 realTopology에 그룹을 부여할 때 실제로 필요한 필드는 **`parentId:'node-airflow-group'`뿐**이며 `group:'airflow'`는 cosmetic(no-op). 두 필드 다 넣어 sample과 형식 정합만 맞추되 동작 원천은 parentId임을 인지한다.
- **③ 동일 파일 편집 충돌·의존**: draft `2026-07-20.bug-buildNodesEdges-groupIds-데드코드.md`(머지완료-통테대기)가 이미 `buildNodesAndEdges.ts` 그룹 생성부를 하드코딩 제거→distinct-`parentId` 순회로 리팩터했다(현 소스가 그 상태). 본 계획의 좌표 상대변환은 그 위에 얹힌다 — 착수 전 draft 머지 반영 여부 확인. 편집 파일 중 `buildNodesAndEdges.ts`↔`buildNodesAndEdges.test.ts`는 동일 파일군 → **직렬/한 에이전트**. `topology.ts`↔`realTopology.ts`는 독립 파일 → **병렬 가능**. e2e spec은 독립.
- **④ 패키지매니저 npm (Verification pnpm 오기)**: 이 repo는 `package-lock.json`+npm. Verification의 `pnpm check`/`pnpm build`는 오기 — 게이트는 `cd frontend && npm run check`(svelte-check)·`npm run build`·`npm run test:unit`(vitest)·`npm run test:e2e`(playwright)를 사용한다.
- **⑤ D-1 미선택 근거**: (a) 전체 그래프 depth 유지 + 상대변환만 → 그룹 폭이 전체 depth 간격(COL_GAP=280)에 종속돼 예측 불가·자식 간 간격 과대 → 탈락. (b) 그룹 자식 0-origin 가로 재배치 채택 — 겹침 근본 차단, 그룹 폭 결정적.
- **⑥ 트리거 UX 이전**: `node-airflow`가 data 뷰에서 숨겨지면 `trigger:true`(topology `:118`) 버튼도 사라진다. 그룹 노드 메타는 현재 `trigger: false`(`:240-241`) — 그룹 노드에 `trigger:true` 부여 + 트리거 액션 배선이 필요하다. 단 그룹 노드 클릭은 `ToolCanvasView.svelte:50`에서 early-return되므로, 트리거 버튼을 그룹 헤더(`AirflowGroupNode.svelte`)에 별도 배치할지 검토(실물 확인 후 결정).

## 작업 목록

### A. buildNodesAndEdges 좌표 상대변환·그룹 박스 (핵심 버그)

- [x] A-1. 그룹 자식 0-origin 가로 재배치(D-1 (b)) 구현 (path: frontend/src/lib/canvas/buildNodesAndEdges.ts, 앵커: `getPosition`/posCache 산출부 `:85-97` 이후 그룹 자식 후처리, 의도: parentId 보유 자식을 그룹별로 모아 `x = idx*COL_GAP`(0-origin), `y=0` 상대좌표로 재계산 — 부모 원점 차감 누락 근본 해소)
- [x] A-2. 그룹 박스 position/크기를 상대좌표 기준으로 재산출 (path: frontend/src/lib/canvas/buildNodesAndEdges.ts, 앵커: `groupNodes.push` 블록 `:216-244` minX/minY/maxX/maxY, 의도: 자식 상대좌표(0-origin)+노드폭(≈200)+패딩으로 width/height 계산해 `extent:'parent'` 클램프가 자식을 접지 않게)
- [x] A-3. 그룹 노드에 `trigger:true` 부여 (path: frontend/src/lib/canvas/buildNodesAndEdges.ts, 앵커: `groupMeta['node-airflow-group']` `:206-214` 및 push data `:238-242`, 의도: node-airflow 숨김 후 트리거 UX를 그룹 노드로 이전 — ⑥ 고려사항)

### B. sample topology 정정 (독립 파일)

- [x] B-1. 독립 `node-airflow` data-edge 제거 + `s3-bronze→docling` 재배선 (path: frontend/src/lib/mock/topology.ts, 앵커: edges `s3-bronze→node-airflow` `:280`·`node-airflow→node-docling` `:283`, 의도: ①권장안 — 두 data 엣지 삭제하고 `node-s3-bronze→node-docling` 추가, node-airflow는 dependency 엣지만 남겨 infra 뷰 보존·data 뷰 orphan 숨김)
- [x] B-2. node-airflow 노드 삭제 여부 확정 (실물 확인 후 결정) (path: frontend/src/lib/mock/topology.ts, 앵커: `node-airflow` 노드 `:116-126`, 의도: ①고려사항 기준 — data-edge만 제거 시 노드 보존이 기본안. 삭제 선택 시 dependency 엣지 재대상 필요)
- [x] B-3. `node-mock-api.tool` 오분류 정정(D-5) (path: frontend/src/lib/mock/topology.ts, 앵커: `node-mock-api` tool `:252`, 의도: `'presidio'` → enrichment tool id로 교체 — D-5 기본안(a) 채택 시 카탈로그 엔트리 추가와 짝)

### C. real topology 그룹 적용 (독립 파일)

- [x] C-1. presidio·docling·kure에 `parentId:'node-airflow-group'`(+`group:'airflow'`) 부여 (path: frontend/src/lib/mock/realTopology.ts, 앵커: `node-presidio`·`node-docling`·`node-kure` 노드 `:106-145`, 의도: sample과 동일 그룹 모델 — 동작 원천은 parentId(②고려사항), 실측값(recognizers=phone,rrn,email,bank_account·nlpEngine=ko_core_news_lg·SequentialExecutor·dagIds·outputDim 384) 보존)
- [x] C-2. node-airflow data-edge 제거 + `s3-bronze→docling` 재배선 (path: frontend/src/lib/mock/realTopology.ts, 앵커: edges `s3-bronze→node-airflow` `:272`·`node-airflow→node-docling` `:275`, 의도: B-1과 동일 패턴, real 고유 엣지(`node-es→node-es-search` `:281`) 보존)
- [x] C-3. `node-mock-api.tool` 오분류 정정(D-5) (path: frontend/src/lib/mock/realTopology.ts, 앵커: `node-mock-api` tool `:244`, 의도: B-3과 동일)

### D. 카탈로그·그룹 노드 컴포넌트 (D-5 (a) 채택 시)

- [x] D-1. enrichment/mock-api 카탈로그 엔트리 추가 (실물 확인 후 결정) (path: frontend/src/lib/canvas/toolCatalog.ts, 앵커: id 목록 `:87-270` 말미, 의도: D-5 (a) 시 mock-api displayName·vendor·icon·accent·category 정의. (b) 채택 시 이 항목 스킵하고 displayNameOverride 유지)
- [x] D-2. AirflowGroupNode 상대변환 후 자식 핸들·nodeclick 실측·보정 (path: frontend/src/lib/components/AirflowGroupNode.svelte, 앵커: 그룹 박스 width/height 렌더 `:18-27`, 의도: D-4 — 자식이 그룹 박스 안에 상대좌표로 정상 배치되고 자식 nodeclick 드릴다운이 동작하는지 e2e 실측 후 필요 시 크기/pointer-events 보정. 트리거 버튼 그룹 헤더 배치 검토(⑥))

### E. 테스트 재작성 (buildNodesAndEdges.test.ts와 동일 파일군 → C계열과 직렬)

- [x] E-1. 그룹 자식 상대좌표·박스 크기 기대값 테스트 재작성 (path: frontend/src/lib/canvas/buildNodesAndEdges.test.ts, 앵커: `Group 노드·parentId·viaTable` describe `:585-651` 및 `group nodes` describe `:655-753`, 의도: 자식 position이 부모 원점 차감된 상대좌표(0-origin)·자식 간 x non-overlap·그룹 박스가 전 자식 포함하는 크기 검증 — 현 C(교차확인) TC가 절대좌표 전제이므로 상대좌표 기준으로 갱신)
- [x] E-2. infra 뷰 회귀 테스트 보강 (path: frontend/src/lib/canvas/buildNodesAndEdges.test.ts, 앵커: infra 뷰 describe `:136-164`, 의도: ①고려사항 — node-airflow가 infra 뷰에 존재하고 dependency 엣지 유지됨을 단언(data-edge만 제거하는 권장안 회귀 방지))

### F. 정리

- [x] F-1. draft `docs/plan/draft/2026-07-20.bug-buildNodesEdges-groupIds-데드코드.md`와 중복/의존 정리 (path: docs/plan/draft/2026-07-20.bug-buildNodesEdges-groupIds-데드코드.md, 앵커: 전체, 의도: ③고려사항 — 해당 draft가 이미 리팩터한 그룹 생성부와 본 계획 좌표 변경의 중복 회피, draft 통테 미완 체크박스(브라우저 확인)와 본 계획 e2e 통합 여부 판단)

### Z. 머지 전·후 검증 (게이트 — 스킵 금지)

#### Z-pre. 머지 전 (워크트리에서 실행)

- (Node 프로젝트 · `node_modules` gitignored → `npm run check`/`build`/vitest/playwright는 Z-pre 제외, 머지 후 원본 main에서 실행 — 아래 Node 정적 게이트·Z-post. 스키마 변경 없음 → 마이그레이션 항목 없음.)
- [x] 정적: 변경 파일 심볼 grep — `parentId` 재산출부·`s3-bronze→docling` 엣지 존재 확인(env 비의존)

#### Z-post. push 후 (앱 기동 환경, 원본 main에서 실행)

- [x] Node 정적 게이트(머지 직후 원본 main): `cd frontend && npm run check` 타입 오류 0 + `npm run build` 성공
- [x] 단위테스트: `cd frontend && npm run test:unit` — E-1/E-2 포함 통과, 통과 개수 기존(~88) 이상(수치 근거 동반) — 95/95 통과
- [x] e2e 통합테스트: `cd frontend && npm run test:e2e`(앱 기동 전제) — 84/84 통과 (8 skipped)
  - [x] `e2e/airflow-group.spec.ts` 신규 작성 — sample(`/sample/pipeline`) 데이터흐름에서 docling·presidio·kure가 Airflow 박스 안에 **서로 다른 x좌표**(겹침 0)로 렌더, 독립 airflow 노드 부재, 그룹 자식 nodeclick 상세 모달 동작 (real 모드는 test 환경에서 백엔드 연결 대기 스텁 — unit test로 커버)
    - teardown: 해당 없음 (읽기전용 렌더 뷰 — DB 레코드/파일 생성 없음)

## TC

> Right-BICEP · CORRECT 기준. 단위(buildNodesAndEdges 순수 함수)+e2e(렌더). DB 무관.

### Right-BICEP

- [x] **Right(정상)**: 그룹 자식(docling·presidio·kure) position이 부모 원점 차감된 상대좌표(0-origin)로 산출, 자식 3개 x가 상호 non-overlap
- [x] **B(경계)**: 자식 1개 그룹 박스 크기 정상 / 자식 0개(parentId 없음)면 그룹 노드 미생성(기존 회귀 `:691-695`) / 빈 topology 크래시 없음(`:104-112`)
- [x] **I(역·부정)**: parentId 없는 노드는 상대변환 미적용(절대 depth 좌표 유지) / node-airflow가 data 뷰에 standalone으로 부재(orphan 숨김)
- [x] **C(교차확인)**: 그룹 박스 width/height가 전 자식 상대좌표+노드폭을 포함해 `extent:'parent'` 클램프에 자식이 안 잘림 / infra 뷰에서 node-airflow·dependency 엣지 유지(①회귀)
- E(에러조건): 미등록 그룹 id fallback(`:225-233`) 유지 — 기존 커버, 순수 함수 throw 경로 없음
- P(성능): 해당 없음 (소규모 정적 토폴로지)

### CORRECT

- [x] **Conformance**: 그룹 자식 position이 상대좌표계(부모 기준) 형식 준수
- [x] **Ordering**: 그룹 내부 자식 x 오름차순(가로 배치) / 데이터뷰 위상정렬 X 단조는 기존 TC(`:82-93`) 커버
- [x] **Range**: 자식 상대 x ∈ [0, groupWidth], y 비음수
- [x] **Existence**: `node-airflow-group` 그룹 노드가 data 뷰에 존재·infra 뷰에 부재(`:605-637` 회귀)
- [x] **Cardinality**: sample·real 모두 그룹 자식 정확히 3개(docling·presidio·kure) / fan-in 3·fan-out 2 회귀(`:274-300`)
- Time: 해당 없음 (시간 의존 로직 없음)

### e2e·DB 통합

- e2e: Z-post에 배치(`e2e/airflow-group.spec.ts` 신규) — 앱 기동 전제, push 후 실행
- DB 통합: 해당 없음 (프론트엔드 순수 함수 + 렌더 e2e, DB·마이그레이션 무관)

## Verification

- **정적**: `cd frontend && npm run check`(svelte-check) 타입 통과, `npm run build` 성공. (repo는 npm+package-lock.json — pnpm 아님, ④고려사항)
- **단위(post-gate, root main)**: `frontend`에서 `vitest run buildNodesAndEdges` — 그룹 자식 상대좌표(부모 원점 차감 확인)·그룹 박스가 자식을 모두 포함하는 크기·겹침 없음(자식 position.x 상호 non-overlap) 검증 케이스 통과.
- **e2e(post-gate)**: `playwright` — sample 모드 데이터흐름에서 docling·presidio·kure가 Airflow 박스 안에 **서로 다른 x좌표**로 렌더(겹침 0), 독립 airflow 노드 부재, real 모드에서도 동일 박스 렌더, nodeclick 상세 모달이 그룹 자식에서 동작.
- **육안**: `/sample/pipeline`·`/real/pipeline` 데이터흐름 뷰에서 Airflow 경계 박스 안 3스테이지가 가로로 나란히, 박스 앞뒤 배선이 `s3-bronze→[박스]→mock-api→{es,mysql}`로 정합.
- **게이트 위치**: `frontend/package.json` 존재 → check/build/vitest/playwright는 워크트리 신뢰 불가 → 머지 후 root main(Z-post). DB·마이그레이션 무관.
