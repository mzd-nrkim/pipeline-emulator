# [BUG] get_gold_staged_documents() 컬럼 불일치 — 항상 빈 배열 반환

> 상태: 통테통과-완료

`ui-backend/app/services/mysql_aggregator.py`의 `get_gold_staged_documents()`가 `gold_staged_documents` 테이블에 존재하지 않는 컬럼(`title`, `priority`, `security_class`, `vehicle_model`, `is_masked`, `stage_reached`)을 직접 SELECT해 MySQL 오류가 발생하고 `except Exception: return []`가 이를 흡수, `/documents` 엔드포인트가 항상 빈 배열을 반환하는 결함이다.

---

## 목표

- `GET /documents`가 실제 gold staged 문서 목록을 반환한다
- 각 문서에 `id`, `title`, `priority`, `security`, `vehicleModel`, `masked`, `stageReached`, `piiCounts` 필드가 정확히 채워진다
- 쿼리가 실제 테이블 스키마와 정합하며, MySQL 오류 없이 실행된다

## 접근 방법

1. **실제 스키마 재확인**: `gold_staged_documents` DDL — `staged_id, enriched_id, es_field_info(JSON), role_ids, metadata_tags, pclrty_class, indexing_status`. `title`·`vehicle_model` 등은 `es_field_info` JSON 내부에 존재.
2. **JOIN 체인 재설계**: 단일 `gold_staged_documents` 조회 대신 아래 체인으로 데이터 추출:
   - `gold_staged` → `gold_enriched` (enriched_id) → `gold_chunked` (chunk_id) → `silver_masked` (masked_doc_id) → `silver_structured` (structured_doc_id) → `bronze_document_hub` (document_hub_hash_key)
   - 단, `gold_staged_documents`는 청크 단위(15행)이므로 **문서 단위로 GROUP BY 또는 MIN/MAX** 집계 필요 (5건 문서 = gold 15행)
3. **필드 추출 전략**:
   - `id`: `bronze_document_hub.source_primary_key`
   - `pclrty_class` → `security`
   - `is_masked`, `pii_pattern_types` → `silver_masked_documents`에서 JOIN
   - `vehicle_model` → `gold_staged.es_field_info` JSON 파싱 (MySQL `JSON_EXTRACT` 또는 Python 파싱)
   - `title`: `silver_structured.structured_content` JSON의 `data.title` 또는 `source_primary_key` 기반 생성
4. **piiCounts 포함**: `silver_masked.pii_pattern_types` JSON을 Python에서 파싱해 `[{type, label, count}]` 변환

## 실행 시 필수 고려사항

① **GROUP BY 키 = `document_hub_hash_key` (또는 `source_primary_key`)** — gold_staged 15행은 청크 단위이므로 문서 대표 1행으로 접기 위해 hub 키로 GROUP BY 해야 한다. `staged_id`로 GROUP BY하면 15행이 그대로 나와 문서 수가 틀어진다. `es_field_info`·`pclrty_class` 등 청크별로 다를 수 있는 값은 `MIN(staged_id)` 기준 행 선택 또는 `ANY_VALUE()`로 대표값을 취한다.

② **`priority` 소스 미결정** — 목표에는 `priority` 필드가 있으나 어떤 테이블에도 `priority`/중요도 컬럼이 gold까지 전파되어 있지 않다. 후보: `gold_staged.es_field_info` JSON 내부 키, 또는 CDC 소스 `source_cft_problem_history.pilot_problem_importnrate_typecd`(S/A/B/C/D/E). 실물 데이터 확인 후 소스가 없으면 현행처럼 기본값 `"C"`를 유지하되 그 사실을 코드 주석으로 명시한다(무언의 하드코딩 금지).

③ **piiCounts 청크 fan-out 중복 위험** — JOIN 체인이 청크 단위로 fan-out되므로 `silver_masked.pii_pattern_types`를 그대로 합산하면 문서당 청크 수만큼 뻥튀기된다. `pii_pattern_types`는 문서(정확히는 `masked_doc_id`) 단위 1행에서만 집계해야 한다.

④ **회귀 원인 = 광범위 예외 흡수** — 원 버그는 `except Exception: return []`가 SQL 오류를 삼켜 침묵 실패한 것. 재작성 시 최소 로깅을 추가해 동일 실패가 다시 빈 배열로 위장되지 않도록 한다.

⑤ **읽기 전용** — 이 변경은 SELECT 경로만 수정한다. DB 레코드 생성/변경이 없어 teardown 대상이 없다.

## 작업 목록

### A. 스키마 정합 JOIN 쿼리 설계

- [x] A-1. 실제 DDL vs 현재 쿼리 컬럼 불일치 목록화 (실물 `db/init.sql` 재확인) (path: db/init.sql, 앵커: gold_staged_documents/silver_masked_documents/bronze_document_hub DDL, 의도: 존재 컬럼 확정 — title·priority·security_class·vehicle_model·is_masked·stage_reached 부재 확인)
- [x] A-2. 문서 단위 GROUP BY 키 결정 (실물 데이터 확인 후 결정) (path: db/init.sql, 앵커: JOIN 체인 종단 bronze_document_hub, 의도: gold 15행→5문서 접기 기준을 `document_hub_hash_key`로 확정, `source_primary_key` 유일성 확인)
- [x] A-3. `priority` 필드 소스 결정 (실물 데이터 확인 후 결정) (path: gold_staged.es_field_info / source_cft_problem_history.pilot_problem_importnrate_typecd, 앵커: 없음, 의도: priority 원천 확정 — 없으면 기본값 "C" 유지 주석 명시)
- [x] A-4. JOIN 체인 SQL 설계 — 문서 단위 GROUP BY 집계 쿼리 작성 (path: 설계 메모, 앵커: 없음, 의도: gold_staged→enriched→chunked→silver_masked→structured→bronze_hub + GROUP BY document_hub_hash_key)
  - [x] `gold_staged.es_field_info`(MIN staged_id 대표) + `pclrty_class` + `is_masked` + `source_primary_key` 셀렉트
  - [x] `pii_pattern_types` JSON 집계 (문서당 1행 `silver_masked` → 청크 fan-out 중복 제거)

### B. 함수 재작성

- [x] B-1. `get_gold_staged_documents()` 쿼리 교체 (path: ui-backend/app/services/mysql_aggregator.py, 앵커: get_gold_staged_documents() 내 cur.execute, 의도: 부재 컬럼 SELECT → JOIN+GROUP BY 쿼리로 교체)
- [x] B-2. `es_field_info` JSON Python 파싱으로 `vehicleModel`·`title` 추출 (path: ui-backend/app/services/mysql_aggregator.py, 앵커: get_gold_staged_documents() 결과 매핑 루프, 의도: json.loads/JSON_EXTRACT, 키 부재 시 fallback)
- [x] B-3. `title` fallback 결정 (실물 확인 후 결정) (path: silver_structured.structured_content, 앵커: get_gold_staged_documents() 매핑 루프, 의도: structured_content JSON `data.title` 있으면 사용, 없으면 source_primary_key 기반 생성)
- [x] B-4. `pii_pattern_types` → `piiCounts` 리스트 변환 (path: ui-backend/app/services/mysql_aggregator.py, 앵커: get_gold_staged_documents() 매핑 루프, 의도: [{type,label,count}] — label 매핑 KR_PHONE→전화번호 등, get_pii_stats()의 기존 JSON 파싱 패턴 재사용)
- [x] B-5. 광범위 예외 흡수 완화 (path: ui-backend/app/services/mysql_aggregator.py, 앵커: get_gold_staged_documents() try/except, 의도: `except Exception: return []`에 최소 로깅 추가 — 침묵 실패 재발 방지)

### Z. 머지 전·후 검증 (게이트 — 스킵 금지)

#### Z-pre. 머지 전 (워크트리에서 실행)

- [x] `python3 -m py_compile ui-backend/app/services/mysql_aggregator.py` 통과
- [x] 격리 단위테스트 — DB 미의존 테스트만 실행. DB 의존 테스트는 Z-post로 강등 (test_nodes.py 7/9 pass; 2건 기존실패 — 68fcb63 nodes.py 포맷 변경, 이번 변경과 무관)

#### Z-post. push 후 (앱 기동 + DB 환경에서 실행)

- [x] 통합테스트: `curl http://localhost:8001/documents` 응답 항목 수 = gold staged 고유 문서 수(5) + 각 항목 `piiCounts` 포함 (러너 에이전트 실행, passed:5/failed:0)
- [x] e2e: frontend documents 소비 페이지가 5건 목록을 렌더 (`npx playwright test` — real-docker-smoke.spec.ts + route-split.spec.ts 12/12 통과)
  - teardown: 읽기 전용 — 생성 레코드 없음, teardown 불필요

## TC (Right-BICEP · CORRECT)

### Right-BICEP

- [x] Right(정상): `curl http://localhost:8001/documents` → 빈 배열(`[]`) 아닌 5건, 각 항목 `id`(문자열)·`security`(RESTRICTED/INTERNAL/PUBLIC)·`masked`(bool)·`vehicleModel`·`piiCounts` 포함
- [x] B(경계): gold_staged 0행 상태 → 빈 배열 정상 반환(오류 아님) — 코드 경로 검증: rows=[] → result=[] → return []
- [x] I(역/교차): 응답 `piiCounts` 합 = `silver_masked.pii_pattern_types` 원천값과 일치 — masked_doc_id별 pii_pattern_types vs API piiCounts 일치 확인
- [x] C(교차확인): 응답 문서 수 = `SELECT COUNT(DISTINCT document_hub_hash_key)` (GROUP BY 정확성) — DB 5건 = API 5건
- [x] E(오류): `es_field_info` 또는 `structured_content` JSON malformed → 해당 문서만 fallback, 전체 쿼리 실패 안 함 — 개별 try/except(JSONDecodeError, ValueError) 코드 경로 검증
- P(성능): 해당 없음 (5건 규모)

### CORRECT

- [x] Conformance: `security` ∈ {RESTRICTED,INTERNAL,PUBLIC}, `masked`=bool, `piiCounts`=list — API 응답 확인
- [x] Existence: `es_field_info` NULL → `vehicleModel` 기본값(NX01), `structured_content`에 title 없음 → source_primary_key fallback, `is_masked=FALSE` 문서 → `piiCounts` 빈 배열 — 5/5 passed
- [x] Cardinality: gold 15행 → 5문서 정확 집계 (청크 fan-out 중복 제거 확인) — 각 문서 chunk_count=3, 5×3=15
- [x] Reference: JOIN 체인 무결성 — `enriched_id`·`chunk_id`·`masked_doc_id`·`structured_doc_id`·`document_hub_hash_key` FK 연결 정상 — 5문서 전체 JOIN 성공
- Ordering: 해당 없음 (정렬 계약 없음) — 필요 시 `source_primary_key` 정렬
- Range / Time: 해당 없음
