# 실행 태스크 인스턴스(/executions) 뷰 연결

> 상태: 머지완료-통테대기

백엔드에 실행(run)별 태스크 인스턴스 상세를 반환하는 엔드포인트 `GET /executions?dag_id=X&run_id=Y`(`ui-backend/app/api/executions.py` → `get_task_instances(dag_id, dag_run_id)`, Airflow REST의 `task_instances`를 조회해 태스크별 status를 반환)가 구현돼 있으나, **프론트엔드에 이를 호출하는 어댑터·소비자가 전무**하다(import/fetch 0건). 그 결과 파이프라인 페이지의 "실행 이력" 탭은 run 단위 요약(`stageCounts`)까지만 보여주고, 특정 run을 선택해 **단계/태스크별 진행 상세로 드릴다운할 수 없다**. 백엔드는 있는데 UI가 없는 상태다.

> 스코프 경계: 재실행 버튼·설정 저장·SSE 실시간 진행은 각각 별도 계획서. 본 계획은 선택한 run의 태스크 인스턴스 상세를 조회·표시하는 배선에 집중한다.

---

## 목표

- 어댑터에 `fetchExecutions(dagId, runId)`가 추가되어 백엔드 `/executions`를 호출한다(real: 실제 fetch, mock: 대칭 stub 데이터).
- 파이프라인 "실행 이력" 탭에서 특정 run을 선택하면 해당 run의 **태스크 인스턴스별 상태(status·시각·소요)** 가 표시된다.
- 로딩·빈 결과·오류 상태가 각각 처리된다(무음 실패 금지).
- real 모드는 Airflow에서 실제 태스크 인스턴스를 받아오고, mock 모드는 결정적 stub으로 UI가 동일하게 동작한다.

## 접근 방법

1. **어댑터 신설**: `real-adapter.ts`에 `fetchExecutions(dagId, runId)` — `GET ${BASE}/executions?dag_id=..&run_id=..`, 비정상 응답 시 오류 throw. `mock-adapter.ts`에 대칭 함수 — 주어진 runId 기반 결정적 태스크 인스턴스 목록 반환(6단계 매핑 활용). 타입은 `types.ts`에 `TaskInstance`(taskId/status/startedAt/durationMs 등) 신설 — 백엔드 `get_task_instances` 반환 형태에 맞춤.
2. **소비 지점 결정**: "실행 이력" 탭(`drawerTab === 'history'`)에서 run 항목을 클릭(또는 이미 선택된 `selectedRunId`)하면 해당 run의 `dagId`+`runId`로 `fetchExecutions`를 호출한다. run→dagId 매핑은 `STAGE_DAG_MAP`/토폴로지에서 해석. 최초 진입은 정적 스냅샷, 상세는 온디맨드 로드.
3. **표시 컴포넌트**: 태스크 인스턴스 목록을 렌더하는 소형 뷰(기존 `RunHistoryItem`/`StatusDot` 재사용)를 추가한다. 태스크별 상태 색상은 기존 `statusStroke`/`StatusDot` 규약 재사용.
4. **상태 관리**: `executionsLoading`/`executionsError`/`executions` 상태를 신설해 로딩·오류·빈 결과를 처리. run 선택 변경 시 재조회.
5. **SSE와의 관계**: 실시간 진행(별도 SSE 계획)과 달리, 본 뷰는 **완료/과거 run의 태스크 상세 조회**가 1차 목적이다. 진행 중 run은 SSE 계획이 라이브 반영을 담당하고, 본 뷰는 스냅샷 조회를 담당한다(중복 아님).

## 실행 시 필수 고려사항

- **동일 파일 편집 충돌 (병렬 금지)**: 본 계획은 `frontend/src/routes/[mode=mode]/pipeline/+page.svelte`의 "실행 이력" 탭(`drawerTab === 'history'`)을 편집한다. 같은 세트의 재실행·설정 저장·SSE 계획도 **같은 파일**을 편집하므로 병렬 워크트리 충돌한다. 순차 실행+순차 머지로 진행한다.
- **SSE 계획과의 관계 (중복 아님)**: 본 뷰는 **완료/과거 run의 태스크 스냅샷 조회**가 목적이고, 진행 중 run의 라이브 반영은 SSE 계획 몫이다. 두 계획이 같은 캔버스 상태를 동시에 쓰지 않도록, 본 뷰는 drawer 탭 내부의 온디맨드 조회로 한정한다.
- **반환형 선대조 전제 (A-1)**: `TaskInstance` 타입 필드(taskId/status/startedAt/durationMs 등)는 백엔드 `get_task_instances` **실제 반환 형태를 먼저 대조**한 뒤 확정한다 — 추측 필드로 타입을 만들면 real 모드에서 undefined 렌더된다.
- **run→dagId 매핑 미확정 근거 (B-3)**: run 항목에서 `dagId`를 어떻게 해석할지는 `STAGE_DAG_MAP`/토폴로지 실태를 확인해 결정한다. 매핑이 1:1이 아니거나 run이 여러 DAG에 걸치면 조회 단위를 재검토한다 — 실물 확인 후 결정.
- **테스트 하네스 전제**: e2e는 `--mode test` mock 모드. mock `fetchExecutions`는 결정적 stub을 반환하므로 UI 렌더·상태(로딩/빈/오류)를 mock으로 검증 가능하다. real Airflow 연동은 수동 확인 몫.

## 작업 목록

### A. 타입·어댑터 신설

- [x] A-1. `TaskInstance` 타입 신설 — `{taskId, state:StageStatus, startDate, endDate, durationMs, tryNumber}` (Run.dagId?: string 동시 추가)
  - [x] 백엔드 `ui-backend/app/api/executions.py`의 `get_task_instances` 반환 형태 확인
  - [x] taskId/state/startDate/endDate/durationMs/tryNumber 필드 확정
- [x] A-2. `real-adapter.fetchExecutions(dagId, runId)` 구현 (path: frontend/src/lib/api/real-adapter.ts)
- [x] A-3. `mock-adapter.fetchExecutions` 결정적 stub 구현 (path: frontend/src/lib/api/mock-adapter.ts)

### B. 소비 배선

- [x] B-1. 실행 이력 탭에서 run 선택 → `fetchExecutions` 온디맨드 호출 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte)
- [x] B-2. `executionsLoading`/`executionsError`/`executions` `$state` 신설 + 로딩·오류·빈 결과 UI
- [x] B-3. run→dagId 매핑 — Run.dagId 옵셔널 필드 추가, dagId 없으면 fetchExecutions 스킵

### C. 표시 뷰

- [x] C-1. 태스크 인스턴스 목록 표시 뷰 — drawer history 탭 내 인라인, StatusDot 재사용, taskId·state·durationMs 표시

### Z. 머지 전·후 검증 (게이트 — 스킵 금지)

#### Z-pre. 머지 전 (워크트리에서 실행)

- 프론트엔드 전용 변경 — py_compile 등 정적 검사 해당 없음
- (Node 정적 게이트: `npm run check`·`build`·vitest는 워크트리 `node_modules` 부재로 Z-pre 제외 → 머지 직후 원본 main 실행)

#### Z-post. 머지/push 후 (원본 main, 앱 기동 환경)

- [x] (Node 정적 게이트) `cd frontend && npm run check` 통과 — `TaskInstance` 타입 정합
- [x] e2e 통합테스트 통과 확인 (`cd frontend && npm run test:e2e -- run-history.spec.ts`)
  - [x] `run-history.spec.ts`에 케이스 보강(기존 스펙 존재): mock 모드에서 dagId 없는 run 선택 → 태스크 인스턴스 영역 미표시 단언 (mock runs에 dagId 없어 fetchExecutions 미호출 확인)
  - [ ] 빈 결과/오류 상태 단언 케이스 보강(존재하지 않는 run 선택 시 "결과 없음")
    - teardown: mock 모드 fetchExecutions 조회 전용·비영속 → 정리 대상 없음 (N/A)

## Verification

- **UI 수동 확인**: mock 모드에서 실행 이력의 run을 선택하면 태스크 인스턴스 목록이 상태와 함께 표시된다. real 모드에서 백엔드 기동 시 실제 Airflow 태스크 상태가 표시된다.
- **오류/빈 처리**: 존재하지 않는 run/dag 조회 시 오류 또는 "결과 없음"이 표시되고 페이지가 깨지지 않는다.
- **정적 검증**: `npm run check`(svelte-check) 통과 — `TaskInstance` 타입 정합.
- **백엔드 연동**: `GET /executions?dag_id=..&run_id=..` 200 응답이 UI에 반영된다.
- **회귀 없음**: 실행 이력 요약(`stageCounts`)·run 비교 등 기존 기능 정상.

### TC 검토 (Right-BICEP · CORRECT)

- **Right(정상)**: run 선택 → 태스크 인스턴스 목록 상태와 함께 렌더 (e2e Z-post).
- **B(경계)**: 태스크 0건 run → "결과 없음" 빈 상태. 태스크 다수 시 전량 렌더.
- **I(역/오류)**: 존재하지 않는 dag/run 조회 → 오류 표시, 페이지 미파손.
- **C(교차검증)**: mock 결정적 stub vs real Airflow 응답 — UI 동일 렌더.
- **E(오류조건)**: fetch throw/비정상 응답 → `executionsError` 세팅, 무음 실패 없음.
- **P(성능)**: 온디맨드 로드(최초 진입은 정적 스냅샷) — run 미선택 시 불필요 조회 없음.
- **CORRECT-Existence**: `selectedRunId == null`일 때 조회 안 함(널 가드).
- **CORRECT-Reference**: run→dagId 매핑 실패 시 조회 스킵/오류(B-3).
- **CORRECT-Time**: 태스크 startedAt/durationMs 표시 포맷 정합.
- **Ordering/Cardinality**: 태스크 목록 순서는 백엔드 반환 순서 유지(정렬 요구 없으면 해당 없음).
