# Airflow 노드/그룹 정체성 통합 + 접힘 시 경계 엣지 재배선

> 상태: 통테통과-완료

data 뷰에서 Airflow가 **두 개의 서로 다른 정체성**으로 모델링돼 있다: `node-airflow`(오케스트레이터 tool 노드 — data 엣지가 없어 orphan) 와 `node-airflow-group`(DAG 태스크 docling·presidio·kure를 감싸는 **합성 그룹 id**). 이 둘은 접기/펴기로 묶인 하나가 아니라 애초에 별개 노드라, "연결 없는 노드 표시"를 켜면 Airflow가 두 번 나타난다. 또한 그룹을 접으면 `buildNodesAndEdges`가 **내부 엣지(양 끝이 자식)만** 제거하고 **경계 엣지**(`s3-bronze → docling`, `kure → mock-api`)는 사라진 자식 id를 계속 가리켜 SvelteFlow가 drop → 접힌 카드가 파이프라인에서 완전히 단절되고 원점(-60,-60)에 겹쳐 렌더된다.

이전 계획서(`2026-07-21.airflow_그룹_접기펴기_config_모달.md`)는 "내부 엣지 숨김 + 자식 제외"만 명세했을 뿐 **정체성 통합**과 **경계 엣지 재배선**을 스펙에 넣지 않았다. 그래서 체크박스는 전부 통과했지만 결과 UX가 요구사항을 만족하지 못한다.

---

## 목표

- data 뷰에서 Airflow를 **단일 정체성**으로 표현한다 — 펼침 = 그룹 박스(내부 DAG 태스크), 접힘 = 단일 노드. 별도 orphan `node-airflow`가 그룹과 중복 표시되지 않는다.
- 그룹을 접어도 Airflow 단일 노드가 **데이터 흐름에 연결된 채로** 남는다: `s3-bronze → [Airflow] → mock-api` 유지.
- 접힌 Airflow 노드가 원점 겹침이 아니라 위상정렬 depth 기준 **흐름상 올바른 위치**에 배치된다.
- infra 뷰의 기존 동작(node-airflow가 오케스트레이터 노드로서 dependency 엣지 유지)은 회귀 없이 보존된다.

## 접근 방법

1. **정체성 통합 — 합성 그룹 id 제거**
   `node-airflow-group`을 없애고 DAG 태스크(docling·presidio·kure)의 `parentId`를 실제 `node-airflow`로 바꾼다(`topology.ts`, `realTopology.ts` 양쪽). 이후 data 뷰의 그룹 컨테이너 = 실제 `node-airflow` 노드. 이로써 "Airflow 하나" 요구를 데이터 모델 수준에서 충족한다.

2. **그룹 메타 파생화 — 하드코딩 제거**
   `buildNodesAndEdges`의 `groupMeta['node-airflow-group']` 하드코딩 맵을 제거하고, 그룹 노드 메타(displayName·icon·accent·role)를 **그룹 컨테이너 노드 자신**(`getToolEntry(node-airflow.tool)` 또는 노드 필드)에서 파생한다.

3. **data 뷰에서 컨테이너 노드의 leaf 렌더 억제**
   data 뷰에서 `node-airflow`는 그룹 박스/접힌 단일 노드로 렌더되므로, leaf tool 노드 배열에서는 제외한다(중복 렌더·orphan 방지). infra 뷰에서는 그룹핑을 하지 않으므로 기존대로 leaf로 렌더한다.

4. **접힘 시 경계 엣지 재배선 (핵심 신규 로직)**
   collapsed 그룹 G의 자식 집합 C에 대해 각 가시 엣지를 분기 처리한다:
   - 양 끝 ∈ C → drop (내부 엣지, 기존)
   - `from ∈ C, to ∉ C` → `(G, to)`로 재배선
   - `from ∉ C, to ∈ C` → `(from, G)`로 재배선
   - 둘 다 ∉ C → 유지
   재배선 결과 동일 `(source,target)` 쌍은 dedupe한다(여러 자식이 같은 외부 노드로 향할 때 중복 방지). 재배선은 **`computeDepths` 이전**에 수행해 접힌 G가 그래프 노드로서 depth 기반 흐름 위치를 얻도록 한다.

5. **접힌 그룹 노드의 위치 산출 변경**
   접힘 시 자식이 leaf 배열에서 제거돼 `absMinX`/`absMinY` 캡처가 불가능하므로, 접힌 그룹 노드 position을 재배선된 그래프의 `computeDepths` 결과에서 가져온다(원점 -60,-60 겹침 제거).

## 작업 목록

### A. 토폴로지 정체성 통합

- [x] A-1. `topology.ts` DAG 태스크 3개 parentId 변경 (path: src/lib/mock/topology.ts, 앵커: node-presidio(L131)·node-docling(L145)·node-kure(L159)의 `parentId` 필드, 의도: 합성 `node-airflow-group` → 실제 `node-airflow`)
  - [x] node-presidio `parentId: 'node-airflow-group'` → `'node-airflow'` (L133)
  - [x] node-docling 동일 변경 (L147)
  - [x] node-kure 동일 변경 (L161)
- [x] A-2. `realTopology.ts` DAG 태스크 3개 parentId 동일 변경 (path: src/lib/mock/realTopology.ts, 앵커: L111·L126·L140의 `parentId`, 의도: 동일 통합)
- [x] A-3. 두 파일의 `group: 'airflow'` 필드·`node-airflow-group` 언급 주석 정리 (path: src/lib/mock/topology.ts + realTopology.ts, 앵커: 자식 노드 정의 블록 주석, 의도: 합성 id 잔재 제거 — grep `node-airflow-group` 0건 확인)

### B. buildNodesAndEdges — 그룹 메타 파생 + 컨테이너 leaf 억제

- [x] B-1. `groupMeta` 하드코딩 맵 제거 + 컨테이너 노드에서 메타 파생 (path: src/lib/canvas/buildNodesAndEdges.ts, 앵커: L227~237 `groupMeta` 리터럴 + L275 `groupMeta[groupId] ?? {...}`, 의도: `getToolEntry(container.tool)` 기반 파생 — displayName·icon·accent·vendor는 카탈로그, role은 컨테이너 노드 role, label은 `${icon} ${displayName}`)
  - [x] 그룹 컨테이너 노드 조회: `topo.nodes.find(n => n.id === groupId)` → `getToolEntry(container.tool)`
  - [x] 파생 실패(엔트리 없음) 시 기존 fallback(`icon:'📦', role:'group'`) 유지
- [x] B-2. data 뷰에서 그룹 컨테이너 노드(`node-airflow`)를 leaf `nodes` 배열에서 제외 (path: src/lib/canvas/buildNodesAndEdges.ts, 앵커: L90 `visibleNodes.filter` 부근 / L148 `visibleNodes.map` 이전, 의도: 그룹 컨테이너 id는 그룹 노드로만 렌더 — data 뷰 && groupId로 쓰이는 노드는 leaf에서 제외해 중복·orphan 방지. 컨테이너 id 집합 = `new Set(topo.nodes.filter(n => n.parentId).map(n => n.parentId))`)
- [x] B-3. infra 뷰 회귀 방지: 그룹 미생성 뷰에서 자식 parentId/extent 미부여 확인 (path: src/lib/canvas/buildNodesAndEdges.ts, 앵커: L161·L166·L180 `nodeGroupId` 분기, 의도: infra 뷰는 group 노드가 없으므로 자식에 `parentId`/`extent:'parent'`를 부여하면 dangling parent → view==='data'일 때만 부여하도록 분기)

### C. buildNodesAndEdges — 경계 엣지 재배선 + 접힘 위치

- [x] C-1. 접힘 시 경계 엣지 재배선 로직 추가 (path: src/lib/canvas/buildNodesAndEdges.ts, 앵커: L92~94 내부 엣지 제외 `visibleEdges.filter` 위치를 확장, computeDepths(L99) 이전, 의도: 자식집합 C에 대해 `from∈C,to∉C`→`(G,to)`, `from∉C,to∈C`→`(from,G)`, 양끝∈C→drop, self-loop(`(G,G)`)→drop)
  - [x] 자식→그룹 매핑 헬퍼: `childId → 소속 collapsed groupId` (기존 `collapsedChildIds` 수집 루프에서 map 동시 구성)
  - [x] 재배선 후 `from===to`(self-loop) 엣지 제거 (컨테이너 G↔자식 엣지 대비 — F3 참조)
- [x] C-2. 재배선 후 `(source,target)` dedupe (path: src/lib/canvas/buildNodesAndEdges.ts, 앵커: C-1 재배선 직후, 의도: 여러 자식이 같은 외부 노드로 향할 때 중복 엣지 제거 — `Set<`${from}->${to}`>` 기준 first-wins)
- [x] C-3. collapsed 그룹 id를 computeDepths 정점 집합에 주입 (path: src/lib/canvas/buildNodesAndEdges.ts, 앵커: L99 `computeDepths(visibleNodes, visibleEdges)` 호출 인자, 의도: node-airflow는 data 뷰 orphan이라 visibleNodes에 없음 → collapsed groupId를 정점으로 추가해야 재배선 엣지로 depth 산출 가능 — F2 참조)
- [x] C-4. 접힌 그룹 노드 position을 depth 기반으로 산출 (path: src/lib/canvas/buildNodesAndEdges.ts, 앵커: L284~290 `isCollapsed ? ... position: {x: absMinX - PAD_X, ...}`, 의도: 접힘 시 자식이 없어 absMinX=0→원점(-60,-60) 겹침 → collapsed면 `getPosition(groupId)`(depth 기반)로 산출)
- [x] C-5. 재배선 엣지 라벨(viaTable) 처리 방침 결정 (실물 확인 후 결정) — first-wins 정책 채택: dedupe 시 첫 엣지 라벨 유지, `{ ...e, from, to }` 스프레드로 원본 viaTable 복사됨

### D. 단위 테스트 (buildNodesAndEdges.test.ts)

- [x] D-0. **기존 픽스처·assertion 정합화 (선행 필수)** (path: src/lib/canvas/buildNodesAndEdges.test.ts, 앵커: `groupTopology`(L610)·`reorderedGroupTopology`(L629) 및 `'node-airflow-group'` 문자열 참조 ~20개, 의도: 통합 반영 — F1 참조)
  - [x] 두 픽스처의 자식 `parentId: 'node-airflow-group'` → `'node-airflow'`
  - [x] 픽스처 엣지를 프로덕션 흐름에 정합: `node-airflow→node-docling` 엣지를 `node-s3-bronze→node-docling` 경계 엣지로 교체(+ node-s3-bronze 노드 추가), 그래야 경계 재배선을 실측 검증
  - [x] 기존 assertion의 `nodes.find(n => n.id === 'node-airflow-group')` 등 문자열을 `'node-airflow'`로 일괄 치환
- [x] D-1. 접힘 시 `s3-bronze → node-airflow`·`node-airflow → mock-api` 재배선 엣지 존재 단언 (Right)
- [x] D-2. 접힘 시 내부 엣지(`docling→presidio`·`presidio→kure`) 미존재 단언
- [x] D-3. 재배선 dedupe: 동일 `(source,target)` 중복 없음 단언 (Cardinality)
- [x] D-4. data 뷰에서 `node-airflow`가 `type:'group'` 노드로만 등장, `type:'tool'` leaf 중복 없음 단언
- [x] D-5. 접힘 시 그룹 노드 position이 원점(-60,-60)이 아님 단언 (C-4 검증)
- [x] D-6. 펼침 ↔ 접힘 왕복 후 엣지·자식 원복 단언 (Inverse — 기존 L805 케이스 확장)
- [x] D-7. 컨테이너 G↔자식 엣지 self-loop 미생성 단언 (C-1 self-loop drop 검증)
- [x] D-8. infra 뷰 회귀: node-airflow dependency 엣지·노드 렌더·자식 parentId 미부여 유지 (기존 E-2 케이스 확장)

### E. e2e 검증 (airflow-group.spec.ts)

- [x] E-1. 접힘 후 `node-airflow` ← `s3-bronze`, → `mock-api` 엣지 DOM 부착 확인 (path: frontend/e2e/airflow-group.spec.ts, 앵커: `test.describe` 내 신규 test, 의도: 접힘 후 경계 재배선 렌더 검증 — SVG는 `toBeAttached`)
- [x] E-2. data 뷰 + "연결 없는 노드 표시" ON 시 Airflow 표현이 **하나만** 존재(중복 orphan 없음)
- [x] E-3. 접힘 후 컴팩트 노드 좌표 sanity: 원점(-60,-60)이 아님(getBoundingClientRect 기반)

### Z. 머지 전·후 검증 (게이트 — 스킵 금지)

#### Z-pre. 머지 전 (워크트리에서 실행)

- [x] 단위테스트 통과 확인 — node_modules gitignored → **Z-post로 강등** (pnpm test는 머지 후 root main에서 실행)
- [x] 정적 검증: `grep -rn 'node-airflow-group' src` 0건(주석·문자열 포함) — 통과 (src + e2e 모두 0건, node-detail-modal.spec.ts 선택자 포함 정정)

#### Z-post. push 후 (앱 기동 환경에서 실행)

- [x] Node 정적 게이트 (머지 직후 원본 main): `pnpm test` PASS (111/111, 541ms), `pnpm run check` (svelte-check) ERRORS 0 / WARNINGS 8 (기존 경고, 변경 무관)
- [x] e2e 통합테스트 통과 확인 (`pnpm exec playwright test e2e/airflow-group.spec.ts`, 앱 기동 전제) — 10/10 PASS (Handle 추가로 E-1 수정); 전체 e2e 90/90 통과(9 skipped = real-docker 환경 필요)
  - [x] E-1·E-2·E-3 신규 케이스가 airflow-group.spec.ts에 추가됨
  - teardown: e2e는 DOM 조회만 수행(DB/파일 생성 없음) → 별도 teardown 불필요, Playwright page 컨텍스트 자동 정리

## 실행 시 필수 고려사항

① **infra 뷰 parentId 처리** — 자식 노드의 `parentId=node-airflow`가 infra 뷰(그룹 노드 미생성)에서 dangling parent를 만들지 않는지 확인한다. 현재도 `node-airflow-group` 참조로 동일 잠재 이슈가 있으므로, 그룹 미생성 뷰에서는 parentId/extent를 부여하지 않도록 분기한다.

② **재배선은 computeDepths 이전** — 접힌 그룹이 depth 그래프의 실제 노드로 참여해야 흐름 위치를 얻는다. 순서가 바뀌면 접힌 노드가 다시 원점에 떨어진다.

③ **엣지 dedupe & 라벨** — 여러 자식이 같은 외부 노드와 연결될 경우 재배선 후 중복 엣지가 생긴다. `(source,target)` 기준 dedupe하되, 서로 다른 viaTable 라벨이 충돌하면 대표 라벨 정책을 정한다(예: 첫 엣지 라벨 유지 또는 라벨 생략).

④ **node-airflow의 이중 역할** — data 뷰 = 그룹 컨테이너 / infra 뷰 = 오케스트레이터 leaf. 두 뷰에서 서로 다른 렌더 경로를 타므로 뷰 분기를 명확히 한다.

⑤ **config 모달 회귀** — 이전 계획서에서 구현한 그룹 제목 클릭 → config 모달(`onTitleClick`/`handleGroupSelect`)이 통합 후에도 `node-airflow` 기준으로 동작하는지 확인한다(groupId가 바뀌므로 `handleGroupSelect`의 자식 수집 로직 점검).

⑥ **접기 어포던스 가시성(별건, 범위 밖)** — expanded 헤더의 접기 버튼이 작은 `▾` 글리프뿐이라 발견성이 낮다. 본 계획 범위에는 넣지 않되, 필요 시 후속 별도 작업으로 분리한다.

⑦ **테스트 픽스처 정합성 — 코드 변경보다 선행되는 리스크(회귀 이유·범위)** — `buildNodesAndEdges.test.ts`의 `groupTopology`(L610)·`reorderedGroupTopology`(L629) 픽스처는 (a) `parentId:'node-airflow-group'`을 하드코딩하고, (b) **프로덕션에 없는** `node-airflow → node-docling` data 엣지를 갖는다. 프로덕션 `topology.ts`의 실제 data 흐름은 `s3-bronze → docling → presidio → kure → mock-api`이며 `node-airflow`는 data 채널 엣지가 **전혀 없는 orphan**(dependency 엣지만 보유)이다. 통합 후 `'node-airflow-group'` 문자열을 참조하는 assertion 약 20개가 전부 깨진다 → D-0에서 픽스처·문자열을 일괄 정합화하지 않으면 단위테스트가 통과할 수 없다. **경계 재배선을 실측 검증하려면 픽스처 엣지를 프로덕션 흐름(`s3-bronze→docling` 진입 경계)으로 맞춰야 한다.**

⑧ **computeDepths 정점 주입 필수(②의 구현 전제)** — 프로덕션 data 뷰에서 `node-airflow`는 orphan이라 `visibleNodes`에 없고 leaf 렌더에서도 제외된다(B-2). 접힘 재배선 엣지가 `node-airflow`(=G)를 참조해도 `computeDepths(visibleNodes,…)` 입력에 G 정점이 없으면 depth가 잡히지 않아 접힌 노드가 다시 원점에 떨어진다. **collapsed groupId를 computeDepths 정점 집합에 명시적으로 추가**(C-3)해야 ②가 실제로 성립한다.

⑨ **컨테이너 G↔자식 엣지 self-loop** — 재배선은 자식 id를 G로 매핑하므로, 한쪽 끝이 컨테이너 G 자신이고 다른 끝이 자식인 엣지는 `(G,G)` self-loop가 된다(테스트 픽스처의 `node-airflow→docling`, 그리고 `kure→mock-api`처럼 `['data','dependency']` dual-channel 엣지 대비). 재배선 규칙에 **self-loop drop**을 반드시 포함한다(C-1).

⑩ **그룹 라벨·아이콘 변경 부수효과(미선택 결정 근거)** — 하드코딩 `groupMeta`는 `displayName:'Airflow (CeleryExecutor)', icon:'🌊'`였으나, `getToolEntry('apache-airflow')` 파생값은 `displayName:'Apache Airflow 3.1.5', icon:'✈️', accent:'#017CEE'`이다. B-1 파생 시 **그룹 박스 표시명·아이콘이 바뀐다**(🌊→✈️). 이는 의도된 SSOT 일원화지만, 그룹 라벨/아이콘을 문자열로 단언하는 기존/신규 테스트가 있으면 함께 갱신해야 한다. 라벨 하드코딩 유지가 요구되면 B-1을 파생 대신 컨테이너 노드 `label`/override 필드 우선으로 조정(실물 확인 후 결정).

⑪ **config 모달 groupId 변경(⑤의 구체화)** — `+page.svelte` `handleGroupSelect`(L61)는 `parentId===groupId` 필터라 groupId가 `node-airflow`로 바뀌어도 자식 수집은 동작한다. 단 `selectedGroup.displayName`이 groupId 문자열(`'node-airflow'`)로 노출되지 않도록 파생 표시명(⑩)과 정합을 점검한다. `ToolCanvasView.svelte`의 `onTitleClick: () => onGroupSelect?.(n.id)`(L50)는 그룹 노드 id를 그대로 넘기므로 별도 수정 불필요.

⑫ **e2e 하네스 전제·teardown** — `airflow-group.spec.ts`는 `/sample/pipeline`(mock 모드)만 검증한다. `real` 모드(`/real/pipeline`)는 test 환경에서 "백엔드 연결 대기" 스텁을 렌더해 파이프라인 노드가 DOM에 나타나지 않으므로(spec 상단 주석 참조) `realTopology.ts` 통합은 단위테스트(D)로만 커버한다. e2e는 DOM 조회 전용 → DB/파일 side-effect 없음 → teardown 불필요.

## Verification

| 항목 | 검증 방법 |
|------|-----------|
| Airflow 단일 정체성 | data 뷰 + orphan 표시 ON에서 Airflow 표현이 1개(그룹/단일)만 존재 — 별도 `node-airflow` orphan 중복 없음 |
| 접힘 시 연결 유지 | Playwright: 접은 후 `s3-bronze → node-airflow`, `node-airflow → mock-api` 엣지 DOM 부착 |
| 접힘 위치 정상 | 접힌 컴팩트 노드가 원점(-60,-60)이 아닌 흐름상 depth 위치 |
| 내부 엣지 제거 | 접힘 시 docling→presidio·presidio→kure 엣지 미존재 |
| 펼침 원복 | 접힘→펼침 왕복 후 자식·엣지·레이아웃 원복 |
| infra 뷰 회귀 없음 | infra 뷰에서 node-airflow dependency 엣지(mysql/seaweed/valkey/es/mock-api → node-airflow) 유지 |
| config 모달 회귀 없음 | 제목 클릭 → 그룹 모달 정상, 트리거 버튼 클릭 → 모달 미열림 |
| self-loop 미생성 | 재배선 후 `source===target` 엣지 0개 (D-7) |
| 단위 테스트 | `pnpm test` PASS (D 케이스 포함) |

### TC 커버리지 점검 (Right-BICEP · CORRECT)

- **Right(정상 경로)**: D-1(재배선 엣지 존재)·D-4(그룹 단일 정체성) / **B(경계)**: D-5(원점 회피)·기존 "빈 토폴로지 크래시 없음"·"존재하지 않는 groupId collapsed" 케이스 유지 / **I(역·부정)**: D-2(내부 엣지 미존재)·D-7(self-loop 미생성) / **C(교차검증)**: D-6 펼침↔접힘 왕복 원복 / **E(에러)**: 기존 미등록 tool 폴백·dangling 엣지 무시 케이스 유지 / **P(성능)**: 해당 없음(소규모 그래프, 결정적 O(V+E)).
- **CORRECT**: **Conformance**(그룹 노드 `collapsed`/`childCount` 형식 — 기존 케이스 유지) / **Ordering**(D-0 정합 후 자식 depth 단조증가 — 기존 E-1 케이스 유지) / **Cardinality**(D-3 dedupe·fan-in/out 개수 — 기존 케이스 유지) / **Reference**(재배선 엣지가 실재 노드 id 참조: `s3-bronze`/`mock-api`/`node-airflow`) / **Existence**(collapsed groupId 정점 존재 → depth 산출, C-3) / **Range·Time**: 해당 없음.
- **e2e**: airflow-group.spec.ts에 E-1·E-2·E-3 **추가**(기존 sample 모드 렌더 케이스 존재).
- **DB 통합 테스트**: **해당 없음** — 순수 프론트엔드 캔버스 렌더링 로직(`buildNodesAndEdges`)으로 DB·백엔드 미접촉.
