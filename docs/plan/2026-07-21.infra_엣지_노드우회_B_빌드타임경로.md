# 인프라 뷰 엣지 노드 우회 — Plan B: 빌드타임 routeY + 런타임 커스텀 컴포넌트

> 상태: 머지완료-통테대기
> 선행 계획: `2026-07-21.인프라뷰_LR계층_꺾은선엣지_재설계.md` (머지완료)

## 문제

LR 계층 배치에서 depth gap > 1인 인프라 엣지(`A depth0 → C depth2` 등)가
중간 컬럼(depth1)의 노드 영역을 관통한다.

**근본 원인**: xyflow 내장 `smoothstep`의 `getSmoothStepPath`는 LR(Right→Left) 모드에서
`center.y`를 무시하고 `verticalSplit` 경로를 생성한다. source·target이 동일 y일 때
verticalSplit이 수평선으로 퇴화해 중간 노드를 그대로 가로지른다.
`pathOptions.offset`/`stepPosition`은 수직 굴절점 x 위치만 조정하므로 이 문제를 해결할 수 없다.

## 목표

- depth gap > 1인 인프라 엣지가 중간 컬럼 노드 영역을 통과하지 않는다.
- 경로는 모든 노드 최상단(y < 0) 위를 통과하는 U자형 라우팅이다.
- **xyflow Handle 앵커 유지**: path 시작·끝이 실제 Right/Left 핸들 좌표에 정확히 고정된다.
- depth gap = 1인 단일 컬럼 엣지는 현행 `smoothstep`을 유지한다.
- 기존 `buildNodesAndEdges` 단위테스트 전량 통과, e2e 시각검증 통과.

## 접근 방법

역할을 둘로 나눈다:

| 책임 | 위치 | 이유 |
|------|------|------|
| **우회 y좌표 결정** (`routeY`) | `buildNodesAndEdges.ts` (빌드타임) | 노드 배치는 빌드타임에 확정된다 |
| **실제 SVG path 생성** | `InfraStepEdge.svelte` (렌더타임) | 핸들 픽셀 좌표(`sourceX/Y`, `targetX/Y`)는 xyflow가 렌더 후 `EdgeProps`로 전달 — 빌드타임에는 알 수 없다 |

빌드타임에 SVG path 문자열을 미리 만드는 방식은 **핸들 좌표를 추정값으로 구워버려 실제 핸들과 어긋난다**. 이 방식을 쓰지 않는다.

### 빌드타임: routeY 계산 (buildNodesAndEdges.ts)

depth gap > 1인 엣지에 대해 `data.routeY`만 계산해 담는다.

```
ROUTE_Y = -48   (노드 상단 y=0 기준, 48px 위)
depthGap = depth.get(e.to) - depth.get(e.from)
if depthGap > 1:
  edge.type = 'infra-step'
  edge.data = { routeY: ROUTE_Y }
else:
  edge.type = 'smoothstep'   (현행 유지)
```

### 렌더타임: path 생성 (InfraStepEdge.svelte)

`EdgeProps`에서 받은 실제 핸들 좌표로 6-point 경로를 그린다.

```
입력: sourceX, sourceY, targetX, targetY  ← xyflow가 실제 Handle에서 계산한 좌표
      data.routeY                          ← 빌드타임에 계산한 우회 y
OFF = 24  (핸들→첫 굴절점 간격)

경로:
  M sourceX sourceY           ← Right 핸들 (앵커 고정)
  L (sourceX+OFF) sourceY     ← 오른쪽으로
  L (sourceX+OFF) routeY      ← 위로 (노드 최상단 위)
  L (targetX-OFF) routeY      ← 수평 우회
  L (targetX-OFF) targetY     ← 타겟 y로
  L targetX targetY           ← Left 핸들 (앵커 고정)
```

꺾임 라운딩은 초기엔 L-join으로 단순 구현, 미관 확인 후 `borderRadius=6` 추가.

### Plan A와의 차이

Plan A는 `useNodes()` store를 읽어 중간 노드를 탐색하고 routeY를 런타임에 계산한다.
Plan B는 routeY를 빌드타임에 고정(`-48`)하고 store를 읽지 않는다. 더 단순하지만 토폴로지가 크게 달라지면 직접 조정이 필요하다.

## 실행 시 필수 고려사항

1. **`depth.get()!` 단언의 암묵적 폴백**: 계획 스펙 `depth.get(e.to)! - depth.get(e.from)!`는 노드가 depth map에 없을 때 `undefined - undefined = NaN` → `NaN > 1 = false`이므로 smoothstep으로 무음 폴백된다. 의도적 안전망이지만 TypeScript 컴파일러가 `!`를 통과시키므로 런타임 경고 없음. D-3 단위테스트로 이 경로를 명시적으로 검증한다.
2. **e2e 삭제 대상 식별 기준**: "line 35-44"는 현재 파일 기준 라인 번호이며 편집 후 달라질 수 있다. 삭제 대상은 테스트명 `'infra-floating 엣지 path가 직선(M ... L ...) 형식이다'` 블록 전체로 식별한다 (실물 확인 후 삭제).
3. **Phase 간 의존성**: A(buildNodesAndEdges.ts)와 B(InfraStepEdge.svelte)는 독립 → 병렬 작업 가능. C(ToolCanvasView.svelte)는 B의 InfraStepEdge 완성이 선행 필요. D(단위테스트)는 A 이후. E(e2e)는 A+B+C 완료 이후.
4. **`edgeTypes` prop 추가 패턴**: `ToolCanvasView.svelte` line 49 `SvelteFlow` 컴포넌트에 `edgeTypes` prop이 현재 없다. `nodeTypes={{ tool: ..., group: ... }}` 옆에 `edgeTypes={{ 'infra-step': InfraStepEdge as any }}` 패턴으로 추가.

## 작업 목록

### A. buildNodesAndEdges.ts — depthGap 분기

- [x] A-1. `ROUTE_Y`, `EDGE_OFF` 상수 추가 (path: frontend/src/lib/canvas/buildNodesAndEdges.ts, 앵커: 파일 상단 import 블록 직후 상수 영역, 의도: routeY 고정값 -48과 핸들 오프셋 24를 상수화해 Magic number 제거)
- [x] A-2. `depthGap` 계산 추가 (path: frontend/src/lib/canvas/buildNodesAndEdges.ts, 앵커: line 162 `visibleEdges.map` 콜백 내부, return 객체 생성 전, 의도: `depth.get(e.to)! - depth.get(e.from)!`으로 source-target 깊이 차이 계산)
- [x] A-3. depthGap 분기로 type·data 할당 (path: frontend/src/lib/canvas/buildNodesAndEdges.ts, 앵커: line 183 `...(view === 'infra' ? { type: 'smoothstep' } : {})` 교체, 의도: infra 뷰에서 gap > 1이면 type='infra-step'+data.routeY 부여, gap ≤ 1이면 smoothstep 유지)

### B. InfraStepEdge.svelte — 신규 커스텀 엣지 컴포넌트

- [x] B-1. 컴포넌트 파일 신규 생성 (path: frontend/src/lib/components/InfraStepEdge.svelte, 앵커: 신규 파일 전체, 의도: xyflow EdgeProps에서 핸들 좌표 수신 후 6-point 우회 path 생성)
  - [x] `<script>` 블록: `EdgeProps`에서 `sourceX, sourceY, targetX, targetY, data, markerEnd` 수신
  - [x] `EDGE_OFF = 24` 로컬 상수 + `data.routeY`로 6-point path 문자열 계산 (`M sourceX sourceY → L (sourceX+24) sourceY → L (sourceX+24) routeY → L (targetX-24) routeY → L (targetX-24) targetY → L targetX targetY`)
  - [x] `<path d={pathD} marker-end={markerEnd} class="svelte-flow__edge-path" />` 렌더링 (라벨 없음)

### C. ToolCanvasView.svelte — edgeTypes 등록

- [x] C-1. `InfraStepEdge` import 추가 (path: frontend/src/lib/components/ToolCanvasView.svelte, 앵커: `<script>` 블록 내 기존 import 영역, 의도: B에서 생성한 커스텀 엣지 컴포넌트 참조)
- [x] C-2. `SvelteFlow`에 `edgeTypes` prop 추가 (path: frontend/src/lib/components/ToolCanvasView.svelte, 앵커: line 49 `SvelteFlow` 컴포넌트의 `nodeTypes={{ ... }}` 옆, 의도: xyflow가 'infra-step' 타입 엣지를 InfraStepEdge로 렌더하도록 등록)

### D. buildNodesAndEdges.test.ts — 단위 테스트 보강

- [x] D-1. depthGap > 1 단언 추가 (path: frontend/src/lib/canvas/buildNodesAndEdges.test.ts, 앵커: 인프라 엣지 관련 기존 테스트 영역 이후, 의도: depthGap=2 엣지가 type='infra-step', data.routeY=-48을 가짐을 단언)
- [x] D-2. depthGap = 1 경계 케이스 추가 (path: frontend/src/lib/canvas/buildNodesAndEdges.test.ts, 앵커: D-1 이후, 의도: gap=1 경계 엣지가 type='smoothstep'이고 data.routeY 없음을 단언)
- [x] D-3. depth map 누락 노드 폴백 케이스 추가 (path: frontend/src/lib/canvas/buildNodesAndEdges.test.ts, 앵커: D-2 이후, 의도: depth map에 없는 노드의 엣지가 NaN > 1 = false → smoothstep으로 폴백됨을 단언)

### E. e2e 테스트 갱신

- [x] E-1. 기존 직선 테스트 삭제 (path: frontend/e2e/infra-view.spec.ts, 앵커: 테스트명 `'infra-floating 엣지 path가 직선(M ... L ...) 형식이다'` 블록 전체, 의도: smoothstep 퇴화 직선 검증 제거 — infra-step으로 대체됨) (실물 확인 후 삭제)
- [x] E-2. depth gap > 1 우회 검증 e2e 추가 (path: frontend/e2e/infra-view.spec.ts, 앵커: E-1 삭제 위치 또는 파일 말미, 의도: 인프라 뷰에서 depth gap > 1 엣지 path의 y 좌표 최솟값이 0 미만임을 확인)
  - teardown: 없음 (read-only e2e)

### Z. 머지 전·후 검증 (게이트 — 스킵 금지)

#### Z-pre. 머지 전 (워크트리에서 실행)

- 스키마 변경 없음 → 마이그레이션 생략
- Node 정적 게이트 규칙 적용 (`frontend/package.json` 존재): vitest·`npm run check`를 워크트리에서 실행하지 않고 Z-post(머지 후 원본 main)로 배치

#### Z-post. 머지 후 (원본 main에서 직렬 실행)

- [ ] vitest 전량 통과: `cd frontend && npm run test:unit`
- [ ] 타입 체크: `cd frontend && npm run check` → 0 errors
- [ ] playwright e2e: depth gap > 1 엣지가 중간 컬럼 노드를 관통하지 않음 (`cd frontend && npx playwright test e2e/infra-view.spec.ts`)
- [ ] playwright e2e: depth gap = 1 엣지 smoothstep 정상 렌더 (회귀 없음)

## 검증 (머지 후)

- [ ] vitest 전량 통과: `cd frontend && npm run test:unit`
- [ ] 타입 체크: `cd frontend && npm run check` → 0 errors
- [ ] playwright e2e: `/sample/pipeline` 인프라 뷰 확인 — depth gap > 1 엣지가 중간 컬럼 노드를 관통하지 않음
- [ ] playwright e2e: depth gap = 1 엣지는 smoothstep 정상 렌더 (기존 동작 회귀 없음)

## TC

### Right-BICEP

- [ ] **Right**: depthGap=2 엣지 → `type='infra-step'`, `data.routeY=-48` (D-1)
- [ ] **Right**: depthGap=1 엣지 → `type='smoothstep'`, data.routeY 없음 (D-2)
- [ ] **Boundary**: depthGap=1(경계, smoothstep) vs depthGap=2(최소 우회, infra-step) 양쪽 모두 단언 (D-1, D-2)
- [ ] **Boundary**: depthGap=0 (자기참조 방어) → type='smoothstep' (D-2에 포함)
- [ ] **Inverse**: infra-step 타입 엣지는 `data.routeY`가 항상 정의됨 (undefined 없음) (D-1)
- [ ] **Cross-check**: depth gap > 1 엣지 count가 빌드타임 반환값과 e2e `.svelte-flow__edge[data-type="infra-step"]` 수와 일치 (E-2)
- [ ] **Error**: depth map에 없는 노드 엣지 → NaN > 1 = false → smoothstep 폴백 (D-3)
- **Performance**: 해당 없음 (빌드타임 O(E) 단순 map)

### CORRECT

- [ ] **Conformance**: `InfraStepEdge.svelte`에서 `data.routeY`가 undefined일 때 방어 — 폴백 routeY 또는 렌더 skip (빌드타임에 보장되나 타입 안전성 명시)
- **Ordering**: 해당 없음 (엣지 순서 무관)
- [ ] **Range**: `routeY=-48`이 현재 최소 노드 y(0) 기준으로 충분히 위임을 단위 테스트에서 수치 단언 (`routeY < 0`) (D-1)
- **Reference**: 해당 없음 (xyflow EdgeProps 타입은 라이브러리 보장)
- [ ] **Existence**: depth map 누락 노드 엣지 → smoothstep 폴백 (D-3에서 커버)
- [ ] **Cardinality**: infra 뷰에서 depthGap > 1인 엣지가 복수(≥2개)일 때 모두 infra-step 타입으로 생성됨 (D-1 픽스처에 엣지 2개 포함)
- **Time**: 해당 없음

## 한계 및 Plan A 전환 기준

- `routeY = -48` 고정값이므로, 모든 노드 위를 지나는 토폴로지에서는 안전하지만 노드가 y < 0에 위치하면 무효. 현재 LR 배치에서 최소 y=0 보장되므로 문제 없음.
- **Plan A 전환 기준**: 미관이 불만족스럽거나, 노드 추가로 routeY 고정값이 부족해질 경우.
