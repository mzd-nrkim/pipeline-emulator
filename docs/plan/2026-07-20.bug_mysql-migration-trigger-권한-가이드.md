# Bug: MySQL 마이그레이션 트리거 생성 계정 권한 가이드 부재

> 상태: 미시작

`002_trigger_cdc.sql`의 `CREATE TRIGGER` 구문은 binlog 활성 환경에서 `SUPER` 권한이 필요하나, `emulator` 계정에 해당 권한이 없어 `ERROR 1419: You do not have the SUPER privilege and binary logging is enabled`가 발생했다. post-gate 실행 시 root 계정으로 재실행해야 했으며, migration 파일·README에 이 제약이 명시되어 있지 않아 재발 위험이 있다.

---

## 목표

- `db/migrations/` 내 DDL 파일에 필요 실행 계정을 주석으로 명시하여 post-gate 실행 가이드를 제공한다.
- `001_polling_index.sql`의 `CREATE INDEX IF NOT EXISTS` MySQL 미지원 문법을 제거하고 멱등 처리 가이드를 포함한다.
- 향후 마이그레이션 작성 시 `emulator` vs `root` 실행 기준 가이드가 `db/migrations/README.md`에 명시된다.

## 접근 방법

1. **`002_trigger_cdc.sql` 주석 추가**: 파일 상단에 `-- 실행 계정: root (SUPER 권한 필요 — CREATE TRIGGER + binlog_enabled)` 주석을 추가해 실행 시 혼선 방지
2. **`001_polling_index.sql` 문법 수정**: `CREATE INDEX IF NOT EXISTS`는 MySQL에서 지원하지 않음 → `CREATE INDEX {name}` + 중복 실행 시 `ERROR 1061` 무시 안내 주석으로 교체 (이미 현재 세션에서 수정됨 — 커밋 반영 여부 확인)
3. **`db/migrations/README.md` 신규 작성**: 마이그레이션 파일 실행 순서·계정별 실행 기준·멱등성 처리 방법을 문서화

## 실행 시 필수 고려사항

### 회귀 이유·범위 — 없음 (문서/주석만 변경)

본 계획은 **DDL 자체를 변경하지 않는다**. `002_trigger_cdc.sql`은 상단 주석만 추가하고(트리거 정의 L15-49는 그대로), `001_polling_index.sql`은 이미 `CREATE INDEX`로 수정 완료된 상태의 확인·커밋 반영일 뿐이며, `README.md`는 신규 문서다. 따라서 스키마 마이그레이션(live 적용)이 필요 없다 — Z-pre 마이그레이션 항목 생략.

### xyflow 세션과의 충돌 — 없음 (프런트엔드 파일 0개)

편집 대상은 `db/migrations/`뿐이며 `frontend/`·Svelte·xyflow 파일을 전혀 건드리지 않는다. xyflow 업그레이드 세션과 **완전 독립** — 지금 즉시 병렬 실행해도 머지 충돌·영향 없음.

### 실물 확인 전제

- `001_polling_index.sql`은 이미 `CREATE INDEX idx_source_cft_upd_dts`(IF NOT EXISTS 제거)로 수정됨(커밋 `6d527b8` 반영). 작업 시 **파일을 먼저 Read해 현재 상태 확인** 후 이미 반영됐으면 체크만 하고 넘어간다.
- `002_trigger_cdc.sql` 상단 L1-2는 현재 `-- F2b Trigger CDC ...` 주석만 있고 실행 계정 주석은 부재.

## 작업 목록

### A. 마이그레이션 파일 주석·가이드 보강

- [ ] A-1. `002_trigger_cdc.sql` 파일 상단(L1 위)에 필요 실행 계정 주석 2줄 추가 (path: db/migrations/002_trigger_cdc.sql, 앵커: 파일 최상단 `-- F2b Trigger CDC` 주석 앞, 의도: root 실행 필요성 명시로 ERROR 1419 재발 방지)
  - `-- 실행 계정: root (binlog 활성 환경에서 CREATE TRIGGER에 SUPER 권한 필요)`
  - `-- emulator 계정으로 실행 시 ERROR 1419 발생 → docker exec ... mysql -uroot -p<root_pass>`
- [ ] A-2. `001_polling_index.sql` 현재 상태 확인 (실물 확인 후 결정 — 이미 `CREATE INDEX`로 수정됐으면 체크만, 미반영이면 `CREATE INDEX IF NOT EXISTS` → `CREATE INDEX` + 멱등 주석) (path: db/migrations/001_polling_index.sql, 앵커: L1-3)

### B. 마이그레이션 README 신규 작성

- [ ] B-1. `db/migrations/README.md` 신규 작성 (path: db/migrations/README.md, 앵커: 파일 신규, 의도: 계정별 실행 기준·순서·멱등성 문서화)
  - [ ] 파일 목록·실행 순서 표 (001 → 002)
  - [ ] 계정별 실행 기준 표 (emulator: 일반 DDL/INDEX, root: TRIGGER·FUNCTION·PROCEDURE — SUPER 권한 필요)
  - [ ] 멱등성 처리 방법 (`CREATE ... IF NOT EXISTS`, `DROP ... IF EXISTS`, INDEX 중복 시 ERROR 1061 무시 패턴)
  - [ ] post-gate 실행 예시 명령 (`docker exec ... mysql -uroot ...`)

### Z. 머지 전·후 검증 (게이트 — 스킵 금지)

#### Z-pre. 머지 전 (워크트리/정적)

- [ ] `head -5 db/migrations/002_trigger_cdc.sql` → 실행 계정 주석 2줄 포함 확인
- [ ] `test -f db/migrations/README.md && grep -q "root" db/migrations/README.md` → README 존재 + 계정 기준 포함 확인
- [ ] `grep -c "IF NOT EXISTS" db/migrations/001_polling_index.sql` → `CREATE INDEX IF NOT EXISTS` 부재 확인(0이어야 함)
- (스키마 변경 없음 → 마이그레이션 live 적용 항목 생략)

#### Z-post. 머지 후 (root main, live DB) — 문서 안내 검증(선택)

- [ ] (선택) `docker exec pipeline-emulator-mysql-1 mysql -uemulator -pemulator_pass pipeline_emulator < db/migrations/002_trigger_cdc.sql 2>&1 | grep -i "ERROR 1419"` → 주석에 안내한 대로 emulator 계정에서 SUPER 권한 오류가 실제 발생함을 확인(주석 정확성 검증). DB 미기동 시 생략 가능.

## TC (Right-BICEP · CORRECT)

문서·주석 변경 성격이라 대부분의 동작 기반 항목은 해당 없음.

### Right-BICEP

- [ ] **Right**: `002_trigger_cdc.sql` 상단에 root 실행 계정 주석이 존재한다.
- [ ] **Right**: `README.md`에 emulator/root 계정별 실행 기준 표가 존재한다.
- [ ] **Boundary**: `001_polling_index.sql`에 MySQL 미지원 `IF NOT EXISTS`가 남아있지 않다(경계: 지원/미지원 문법).
- [ ] **Error**: (Z-post 선택) emulator 계정 실행 시 ERROR 1419가 주석 안내대로 재현된다.
- **Boundary(그 외)/Inverse/Cross-check/Performance**: 해당 없음(정적 문서).

### CORRECT

- [ ] **Conformance**: README의 멱등 패턴(`DROP TRIGGER IF EXISTS`, INDEX ERROR 1061 무시)이 실제 SQL 파일 구현과 일치한다.
- [ ] **Reference**: README가 참조하는 파일명(`001_polling_index.sql`, `002_trigger_cdc.sql`)이 실제 존재 파일과 일치한다.
- **Existence/Cardinality/Ordering/Range/Time**: 해당 없음.

teardown: 생성·변경 side effect 없음(파일 편집·문서 작성만) → 해당 없음.
