# DAG 뷰 노드상세·실행이력 사이드바 → 모달 전환

> 작성일: 2026-07-20 / 상태: 통테통과-완료
> 대상: `frontend/src/routes/[mode=mode]/pipeline/+page.svelte`

파이프라인 DAG 뷰에서 노드를 클릭하면 나오는 "노드 상세 / 실행 이력"이 현재 우측 고정 오버레이 드로어(사이드바, `w-80`)로 표시된다. 이를 뷰포트 중앙 모달(Dialog)로 전환한다. 사이드바는 폭(20rem)이 좁아 실행 이력의 비교(diff) 뷰가 답답하고, 캔버스를 가려 노드 배치 파악을 방해한다. 모달은 넓은 표시 공간과 배경 딤 처리로 두 문제를 해소한다.

---

## 목표

- 노드 클릭 또는 "실행 이력" 열기 시, 우측 드로어 대신 중앙 모달(Dialog)로 노드 상세·실행 이력이 표시된다.
- 모달은 노드 상세 / 실행 이력 두 탭을 그대로 유지하며, 기존 콘텐츠(설정 폼·DAG 트리거·실행 이력 diff)가 동일하게 동작한다.
- 모달은 배경 오버레이(딤)·ESC·바깥 클릭·닫기 버튼으로 닫히고, 접근성(포커스 트랩·`aria`)을 갖춘다.
- 실행 이력 비교 뷰가 사이드바 대비 넓은 폭에서 쾌적하게 보인다.

## 접근 방법

1. **컴포넌트 선택**: 이미 의존성에 포함된 `bits-ui`(v1.8.0)의 `Dialog`를 사용한다. 신규 라이브러리 도입 없이 `Dialog.Root / Trigger / Portal / Overlay / Content / Close` 프리미티브로 구성한다. 별도 wrapper 컴포넌트(`src/lib/components/ui/Dialog.svelte` 등 스타일 프리셋)를 만들지, `+page.svelte`에 인라인으로 둘지는 재사용 필요성 기준으로 결정한다(현재 사용처가 1곳이므로 인라인 우선, 추후 추출).

2. **상태 매핑**: 기존 상태 변수를 Dialog 제어로 연결한다.
   - `drawerOpen`(boolean) → `Dialog.Root`의 `open` 바인딩(`bind:open`). 변수명은 `dialogOpen`으로 정리하거나 기존명 유지.
   - `drawerTab`('node' | 'history') → 모달 헤더의 탭 전환 상태로 그대로 사용.
   - `selectedNode`, `triggeredRunId`, `triggerError`, 비교용 `selectedRunId`/`selectedRunIdB` → 변경 없음(콘텐츠 로직 재사용).
   - `handleNodeSelect`: 노드 선택 시 `dialogOpen = true` + `drawerTab = 'node'` 세팅(현재는 드로어가 상시 존재해 `drawerOpen`을 별도로 안 켰을 수 있음 — 열림 트리거 명시).

3. **마크업 이식**: 현재 드로어 블록(약 L160~522)의 헤더(탭+닫기)와 콘텐츠(노드 상세 탭 / 실행 이력 탭)를 `Dialog.Content` 내부로 옮긴다.
   - 컨테이너 클래스 교체: `absolute right-0 top-0 h-full w-80 ...` → 모달용 중앙 배치·최대폭(예: `max-w-3xl w-[90vw] max-h-[85vh]`)·`overflow-auto`. 실행 이력 diff를 고려해 노드 상세보다 넓은 폭 확보.
   - 닫기 버튼(`✕`)은 `Dialog.Close`로 대체. 노드 상세 탭 내부의 "노드 선택 해제(`selectedNode = null`)" 버튼은 모달을 닫지 않고 노드만 해제하는 기존 동작 유지(모달 닫힘과 구분).

4. **트리거 진입점 정리**: 현재 드로어를 여는 경로를 점검한다 — (a) 노드 클릭(`onnodeselect`→`handleNodeSelect`), (b) "실행 이력" 별도 열기 버튼이 있는지. 각 진입점이 `dialogOpen`을 켜도록 통일한다.

5. **접근성·UX**: bits-ui Dialog가 포커스 트랩·ESC·`aria-modal`·바깥 클릭 닫기를 기본 제공하므로 수동 `aria-label`만 유지·보완. 배경 `Dialog.Overlay`에 딤 스타일 적용. Portal 사용으로 z-index 이슈(기존 `z-20`) 해소.

6. **정리**: 전환 후 사용하지 않는 드로어 전용 클래스·주석("오버레이 드로어")·죽은 상태 변수를 제거한다.

## 실행 시 필수 고려사항

> plan-review 검토 발견 사항. plan-run이 참조한다.

### ① 회귀 이유·범위 — 기존 e2e `run-history.spec.ts` 셀렉터 파손

- **현 구조**: `+page.svelte`의 `drawerOpen` 초기값이 `true`(L25), `drawerTab` 초기값이 `'history'`(L26) → 페이지 로드 시 드로어가 **상시 열린 상태**이고 "실행 이력" 탭 버튼이 항상 보인다.
- **기존 e2e 의존**: `e2e/run-history.spec.ts`는 5개 테스트 전부 `page.locator('button').filter({ hasText: /실행 이력/ })`(L11·37·67·107·136)로 "실행 이력" 버튼을 찾아 클릭한다. 이 버튼이 상시 노출됨을 전제한다.
- **파손 메커니즘**: 모달 전환 후 "실행 이력" 탭 버튼은 `Dialog.Content` 내부로 이동한다. 모달은 기본 닫힘 → 페이지 로드 직후 이 버튼이 DOM에 없거나 비가시 → e2e 클릭이 타임아웃한다.
- **대응**: 페이지 본문(캔버스 영역)에 **모달을 여는 "실행 이력" 진입점 버튼을 신규로 남긴다**(닫힌 상태에서 항상 가시). 이 버튼 텍스트에 "실행 이력"을 포함시켜 기존 e2e 셀렉터와 호환시키거나, 호환 불가 시 `run-history.spec.ts`의 진입 로직을 모달 오픈에 맞게 갱신한다(둘 중 실물 확인 후 결정).

### ② 초기 표시 동작 변경 — 상시 오픈 → 명시적 트리거

- 드로어는 초기 오픈이라 실행 이력이 첫 화면부터 보였다. 모달은 기본 닫힘이 자연스러우므로 **초기 표시 동작이 바뀐다**. 이 UX 변경(첫 진입 시 실행 이력이 자동으로 안 보임)이 의도된 것인지 재확인 — 필요 시 첫 진입만 자동 오픈할지 결정(실물 확인 후 결정).

### ③ 재열기 진입점(`▶` 버튼) 대체

- 현재 드로어 닫힘 시 재열기용 `▶` 버튼(L514~521, `onclick={() => drawerOpen = true}`)이 존재한다. 모달 전환 후 이 버튼은 의미가 사라진다 → ①의 "실행 이력 열기" 진입점으로 대체·통합한다.

### ④ bits-ui Dialog 첫 도입 — 참고 사용처 없음

- 프로젝트 내 `bits-ui` import 사용처가 0곳(`grep "from 'bits-ui'"` 무결과). `Dialog.Portal` 사용 시 오버레이가 `<body>` 말단에 렌더되어 기존 `z-20` 계층 밖으로 나간다 → z-index 충돌은 해소되나, Tailwind 다크 테마 토큰(`bg-surface`·`border-border`)이 Portal 밖에서도 정상 적용되는지 확인 필요.

### ⑤ 테스트 하네스·환경 전제 (Node 정적 게이트)

- `node_modules`는 gitignore됨(`pipeline-emulator/.gitignore` L1). 워크트리에는 `node_modules`가 없다 → `npm run check`/`build`/`test:unit`(vitest)는 **워크트리(Z-pre)에서 실행 불가**, 머지 후 원본 main에서 실행한다.
- e2e(`playwright`)는 `webServer`로 `npm run dev -- --port 5177 --mode test`를 기동(`playwright.config.ts`) → 앱 기동 전제이므로 **Z-post**에서 실행.

## 작업 목록

### A. Dialog 도입 및 상태 매핑

- [x] A-1. bits-ui `Dialog` 프리미티브 import 및 사용 형태 확정 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: `<script>` 상단 import 구역, 의도: `Dialog.Root/Portal/Overlay/Content/Close` 프리미티브 도입)
  - [x] `import { Dialog } from 'bits-ui'` 추가 (사용처 1곳이므로 인라인 우선 — wrapper 컴포넌트 미생성, 실물 확인 후 결정)
- [x] A-2. 상태 변수를 Dialog 제어로 매핑 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: L25 `drawerOpen` 상태 선언, 의도: `Dialog.Root`의 `bind:open` 소스로 연결)
  - [x] `drawerOpen`(L25) → `Dialog.Root open={...} onOpenChange`/`bind:open` 매핑 (변수명 `dialogOpen`으로 정리 또는 기존명 유지)
  - [x] 초기 오픈 동작 결정: 모달 기본 닫힘으로 초기값 조정 여부 반영 (고려사항 ② — 실물 확인 후 결정)
- [x] A-3. 열림 트리거 통일 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: L32~37 `handleNodeSelect`, 의도: 진입점이 `dialogOpen=true`를 켜도록 통일)
  - [x] `handleNodeSelect`에 `dialogOpen = true` + `drawerTab = 'node'` 추가 (노드 클릭 시 모달 오픈)
  - [x] 페이지 본문에 "실행 이력 열기" 진입점 버튼 신규 추가 (고려사항 ①③ — 닫힌 상태에서 상시 가시, 텍스트에 "실행 이력" 포함해 기존 e2e 셀렉터 호환)
  - [x] 기존 재열기 `▶` 버튼(L514~521) 제거 또는 위 진입점으로 대체 (실물 확인 후 결정)

### B. 마크업 이식

- [x] B-1. 드로어 헤더(탭+닫기) → `Dialog.Content` 헤더로 이식 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: L163~190 드로어 헤더 블록, 의도: 탭 UI 유지·닫기 버튼 `Dialog.Close`화)
  - [x] 탭 버튼(노드 상세 L167~172 / 실행 이력 L174~180) `Dialog.Content` 헤더로 이동
  - [x] 닫기 버튼(✕, L182~189)을 `Dialog.Close`로 대체
- [x] B-2. 노드 상세 탭 마크업 이식 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: L196~331 `drawerTab === 'node' && selectedNode` 블록, 의도: 콘텐츠 로직 재사용·모달 내부 배치)
  - [x] "노드 선택 해제" 버튼(L205, `onclick={() => selectedNode = null}`)이 모달을 닫지 않고 노드만 해제하는 동작 유지 (모달 닫힘과 구분)
- [x] B-3. 실행 이력 탭 마크업 이식 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: L334~508 `drawerTab === 'history'` 블록, 의도: 비교(diff) 뷰를 넓은 모달 폭에 배치)
  - [x] `selectedRunId`/`selectedRunIdB` 비교 diff가 넓은 폭에서 정상 레이아웃되는지 확인

### C. 모달 스타일·접근성

- [x] C-1. 모달 컨테이너 스타일 적용 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: 이식된 `Dialog.Content` 컨테이너, 의도: 드로어 클래스 `absolute right-0 top-0 h-full w-80 z-20`(L162) → 중앙 배치·최대폭)
  - [x] `Dialog.Content` 클래스: 중앙 배치 + `max-w-3xl w-[90vw] max-h-[85vh]` + `overflow-auto` (실행 이력 diff 고려해 넓은 폭)
  - [x] `Dialog.Overlay`에 배경 딤 스타일 적용
- [x] C-2. 접근성·Portal 확인 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: `Dialog.Root`/`Portal`/`Overlay`, 의도: 포커스 트랩·ESC·aria-modal·바깥 클릭 기본 동작 검증)
  - [x] `Dialog.Portal` 사용 시 다크 테마 토큰(`bg-surface`·`border-border`)이 Portal 밖에서도 적용되는지 확인 (고려사항 ④)
  - [x] 수동 `aria-label` 유지·보완

### D. 정리

- [x] D-1. 사이드바 전용 잔여 제거 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: 전환 전 드로어 블록 L161~511 잔여, 의도: 죽은 코드·주석·상태 제거)
  - [x] 드로어 전용 클래스(`w-80`·`absolute right-0` 등)·"오버레이 드로어" 주석 제거
  - [x] 전환 후 미사용 상태 변수 제거 (grep으로 참조 0 확인 후)
- [x] D-2. 반응형(작은 뷰포트) 표시 확인 (path: frontend/src/routes/[mode=mode]/pipeline/+page.svelte, 앵커: `Dialog.Content` 반응형 클래스, 의도: `w-[90vw]`가 좁은 화면에서 넘치지 않는지 확인)

### Z. 머지 전·후 검증 (게이트 — 스킵 금지)

#### Z-pre. 머지 전 (워크트리에서 실행)

- [x] 정적 검증: 사이드바 전용 클래스(`w-80` 드로어)·"오버레이 드로어" 주석·제거 대상 상태 변수를 grep해 잔여 0 확인
- [x] 이식 후 참조 무결성 grep: 제거한 변수/함수의 잔여 참조 0 확인
- (Node 정적 게이트: `npm run check`/`build`/`test:unit`는 워크트리에 `node_modules`가 없어 Z-pre에서 제외 → 머지 후 원본 main에서 실행 — 고려사항 ⑤)

#### Z-mid. 머지 직후 (원본 main, node_modules 상주)

- [x] `cd frontend && npm run check` — svelte-check 타입 에러 0
- [x] `cd frontend && npm run build` — 컴파일 에러 0
- [x] `cd frontend && npm run test:unit` — vitest 통과 (기존 단위테스트 회귀 없음)

#### Z-post. push 후 (앱 기동 환경에서 실행)

- [x] e2e 통합테스트 통과 확인 (`cd frontend && npm run test:e2e`, 앱 기동 전제 — `webServer`가 `--port 5177 --mode test`로 dev 서버 기동)
  - [x] 기존 `e2e/run-history.spec.ts` 통과 확인 — 모달 진입점 변경 반영해 갱신 필요 시 수정 (고려사항 ① — "실행 이력" 진입 셀렉터가 모달 오픈으로 동작)
  - [x] 기존 `e2e/node-config.spec.ts` 통과 확인 — 노드 클릭→모달 오픈→설정 폼/저장 버튼 접근 경로 유지
  - [x] `e2e/node-detail-modal.spec.ts` 신규 작성 (모달 전환 전용 시나리오, 미존재 시 필수)
    - 노드 클릭 → 중앙 모달 오픈 + "노드 상세" 탭 활성 (우측 드로어 아님)
    - "실행 이력" 탭 전환 → 실행 목록·비교 diff 가시
    - ESC / 바깥(오버레이) 클릭 / `Dialog.Close`(✕) 각각으로 모달 닫힘 검증 (3 케이스)
    - 모달 오픈 중 배경 캔버스 딤·pointer 차단 확인
    - "노드 선택 해제" 버튼 클릭 시 모달은 유지되고 노드만 해제됨 확인
    - teardown: 이 스펙은 읽기·UI 상호작용만 수행하며 DB 레코드·파일을 생성하지 않음 → teardown 불필요 (해당 없음)

## TC

> Right-BICEP · CORRECT 기준. 순수 프론트엔드 UI 전환(DB 스키마 변경 없음)이므로 데이터 관련 항목은 해당 없음 처리.

### Right-BICEP

- **Right (결과 정합)**: 모달 오픈 시 `drawerTab` 초기값에 따라 올바른 탭이 활성화되고, 이식 콘텐츠(설정 폼·DAG 트리거·diff)가 드로어 시절과 동일하게 렌더된다.
- **B (경계 조건)**:
  - [x] `selectedNode === null` 상태에서 "노드 상세" 탭이 disabled 유지되는지 (기존 탭 disabled 로직 보존)
  - [x] 실행 이력이 0개일 때 모달 실행 이력 탭 레이아웃 깨짐 없음
  - [x] 매우 긴 실행 이력/넓은 diff에서 `max-h-[85vh]` + `overflow-auto` 스크롤 정상
- **I (역 관계)**: 모달 열기(`dialogOpen=true`) ↔ 닫기(ESC·오버레이·Close) 왕복 후 상태 일관성 (재오픈 시 잔존 상태 오염 없음).
- **C (교차 확인)**: bits-ui Dialog 기본 포커스 트랩·`aria-modal` 동작을 수동 검증(브라우저 Tab 순환)으로 교차 확인.
- **E (에러 조건)**:
  - [x] DAG 트리거 실패 시 `triggerError`가 모달 내부에 정상 표시 (드로어 시절 동작 보존)
- **P (성능)**: 해당 없음 (렌더 경로 변경 미미, 단일 모달).

### CORRECT

- **C (Conformance)**: 이식 마크업이 기존 Tailwind 토큰·클래스 규약을 따름.
- **O (Ordering)**: 해당 없음 (순서 의존 상태 없음 — 탭 전환은 순서 무관).
- **R (Range)**: `w-[90vw]`·`max-w-3xl`이 좁은(모바일)~넓은(데스크톱) 뷰포트 범위에서 넘침 없이 표시 (D-2·반응형 TC와 동일).
- **R (Reference)**: 모달은 `selectedNode`·실행 이력 데이터에 의존 — 데이터 미로드 시 참조 오류 없음.
- **E (Existence)**: `selectedNode`·`triggeredRunId`·`triggerError`가 null/undefined일 때 모달 콘텐츠 방어 렌더.
- **C (Cardinality)**: 실행 이력 diff 비교 대상 0/1/2개(단일·비교) 각 케이스 표시 정상.
- **T (Time)**: 해당 없음 (시간 의존 로직 없음).
